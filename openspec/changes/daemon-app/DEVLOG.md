# DEVLOG — daemon-app

Narrative companion to `tasks.md`. Newest entries at the bottom of each section.

## Pinned decisions (read before planning any block)

- **ADR-028 ACCEPTED** (`docs/adrs/ADR-028-daemon-bucket.md`, indexed in `CLAUDE.md`). It expanded the originally-scoped "daemon bucket" decision into the full personal-assistant topology:
  - **`daemon/` bucket** = Daemon *composition only*: `Daemon.cs`, `Daemon.Routing`, `Daemon.App` (dmonium). **Not** a backing server.
  - **NEW `services/` bucket** = standalone backing servers that pair with a `tools/` extension. Members: `services/Dcal` (the iCal-sync HTTP server, **moved+renamed** from `daemon/Daemon.Calendar`) and the future `services/Dmail` (grafted later from its own repo). Services are **app artifacts, independently versioned, NOT NuGet/protocol-keyed** (off ADR-024's lockstep train).
  - **`dmonium` → `daemon/Daemon.App`** (not `frontends/`, which stays protocol-surface hosts).
  - **`dcal` rename** (matches the shipped `DCAL_*` config): server `services/Dcal` (`Dcal.csproj`), tool `tools/Dmon.Tools.Dcal` (verb `AddDcalAbilities()`, types `DcalExtension`/`DcalAbilityProvider`), standing specs `dcal-lookup`/`dcal-sync`. **Model-facing tool names (`lookup_calendar`, `list_upcoming_events`) and the `DCAL_*` prefix are UNCHANGED** — rename identifiers only, keep domain words.
  - **Swift** is supported in-repo but **outside** `Everything.slnx`/`daemon.slnx`; built via `make daemon-app`.
- **Scope decision (user):** the `services/` bucket + `dcal` rename are **folded into this change** (not a separate change). That is why `tasks.md` gained section 2 and the spec set gained a `monorepo-layout` MODIFIED delta.
- **Resolved Open Questions:** OQ-A (ADR number) → new **ADR-028**, Accepted. ADR-025 OQ-B → resolved for dmonium + Swift + Dmail-server home; `dmon-swift` SDK still open.
- **Prereqs present (confirmed):** Phase 1 seams `ITerminalClientFactory`, `IAbilityProvider`, `AbilityRegistry` all exist in `core/`. **CORRECTION:** there is **no** `DaemonTelemetry` in core — the core telemetry class is `DmonTelemetry` (`core/Dmon.Core/Telemetry/DmonTelemetry.cs`) and it has **no** triage counter. The `dmon.triage.misclassify.personal_to_world` counter is owned by a **`Daemon.Routing`-defined `DaemonTelemetry`** static class (created in §4); `Daemon.Routing`'s `Dmon.Core` ProjectReference is for `AbilityRegistry` only. (R7 / tasks 4.1/4.4/4.7 wording corrected accordingly.) The `ITerminalClientFactory` hook is already wired in `TurnHandler` (optional DI ctor param) — registering the factory is the entire hook; do not touch `TurnHandler`/`Build()`. Phase 2 calendar code exists: `tools/Dmon.Tools.Calendar` and `daemon/Daemon.Calendar` (the latter about to move to `services/Dcal`), both already in `Everything.slnx`.
- **Standing specs `calendar-lookup`/`calendar-sync` NOT yet renamed** — left matching the current (pre-rename) code; task 2.6 renames them in lockstep with the code rename (task 2.4). Don't be surprised they still say "calendar".
- **Gotcha:** `make test` hangs ~90s if `MEKO_API_KEY` is set (live Meko smoke). Brief workers to run `env -u MEKO_API_KEY make test`.

## Section 1 — ADR-028 (DONE)

- 1.1/1.2 complete: ADR-028 authored and accepted by the user (after two revisions — the services bucket + dcal rename were added at the user's direction during review); `CLAUDE.md` ADR index row added. The change's `proposal.md`/`design.md`/`tasks.md`/specs were realigned to the expanded scope and `openspec validate daemon-app --strict` passes.

## Section 2 — services/ bucket + dcal rename (DONE)

- **2.1–2.7 complete; reviewer APPROVED (no blockers).** All renames landed as `git mv` R100 (history preserved). Gates green: `make build` 0/0, `env -u MEKO_API_KEY make test` all pass (Dcal.Tests 13/13, Dmon.Tools.Dcal.Tests 14/14, Core 591, Terminal 187, Gateway 208, Mtplx 32), `validate --strict` valid.
- Final layout: server `services/Dcal` (`Dcal.csproj`, `Sdk=Microsoft.NET.Sdk.Web`, `InternalsVisibleTo=Dcal.Tests`); tool `tools/Dmon.Tools.Dcal` (verb `AddDcalAbilities()`, types `DcalExtension`/`DcalAbilityProvider`, file `DcalRegistrationExtensions.cs`, namespace `Dmon.Hosting` retained); specs `dcal-lookup`/`dcal-sync`; new root `services.slnx` + `services/README.md`; `daemon.slnx` reduced to a valid EMPTY `<Solution>` (section 3 repopulates with `Daemon.Routing`); `Everything.slnx` `/daemon/` folder now empty, `/services/` folder added.
- **Kept (domain words, NOT renamed):** tool names `lookup_calendar`/`list_upcoming_events`, `DCAL_*` prefix, domain types `CalendarEvent`/`CalendarClient`/`ICalendarStore`/`CalendarApiException`/`CalendarRow`/`CalendarDatabase`/`CalendarSyncService`, test fixture class names (`CalendarExtensionTests` etc.), and `DcalExtension.Name => "Calendar"`. Reviewer confirmed this is correct per "rename identifiers only."
- For §3's architect: `daemon.slnx` exists at root but is currently EMPTY; §3 adds `Daemon.Routing` to it. `daemon/` currently holds only `BRIEF.md` (pre-existing tracked file).

- **Convention ruling (orchestrator, verified against the repo):** all area `.slnx` live at **repo ROOT** (`core.slnx`, `daemon.slnx`, `tools.slnx`, …) — there are **no in-bucket `.slnx`**, and there is a **single root `Directory.Build.props`**. So `services.slnx` is created at root (not `services/services.slnx`), and **no** `services/Directory.Build.props` is added (root covers it; ADR-025 D8 makes per-area props optional). Tasks 2.1/3.1/3.2 were corrected from the original in-bucket wording to match this. `daemon.slnx` **already exists** at root (block-1 architect's "no daemon/daemon.slnx" was about the in-bucket path only). NOTE for §3's architect: task 3.4 still calls for a `daemon/Directory.Build.props` to default `Daemon.*` assembly names — that IS a legitimate divergent per-area prop, so it's allowed; it would be the repo's first per-area props file.

## Section 4 — Daemon.Routing triage router (DONE)

- **4.1–4.8 complete; reviewer APPROVED after one fix round.** New `daemon/Daemon.Routing` library: `RouteContracts.cs` (`enum Tier`, `record RouteDecision`, `record TriageOptions{EgressThreshold=0.8f}`), `TriageRouter.cs` (`sealed : DelegatingChatClient`, shared `ClassifyAndRouteAsync` helper for both response paths), `DaemonTelemetry.cs` (own `Meter "Daemon.Routing"` + counter `dmon.triage.misclassify.personal_to_world` — core `DmonTelemetry` untouched), `TriageRegistrationExtensions.cs` (`UseTriage`/`AddReasoner`/`AddEgress` on `IDmonHostBuilder` in `namespace Dmon.Hosting`, `TriageRouterFactory : ITerminalClientFactory`, + `internal sealed record` wrappers `E2bRawClient`/`ReasonerClient`/`EgressClient` for unambiguous DI). **`daemon.slnx` is no longer empty — it now contains `Daemon.Routing` (this satisfies the substance of tasks 3.1/3.2 for that project).**
- **Key facts for later blocks:** the `ITerminalClientFactory` hook is already wired in `TurnHandler` (optional DI ctor param) — registering the factory IS the hook; `Build()`/core untouched. `e2bWithTools = e2bRaw.AsBuilder().UseFunctionInvocation().Build()` and `classifier = e2bRaw` are built in the FACTORY. R5 switch: backend off the RAW decision, manifest off the EFFECTIVE (post-gate) scope. `IsPackable=false` (off the protocol-lockstep train).
- **Review fixes applied (both landed):** B1 — use `ChatOptions.Clone()` for the scoped-options copy (the hand-rolled copy dropped `RawRepresentationFactory`/`ContinuationToken`/`AllowBackgroundResponses` and downcast derived options); A1 — null-coalesce the classify result to a confident-personal default (`new RouteDecision("personal", Tier.Direct, false, 1f)`) so an unparseable classification fails safe and never trips the counter. Non-blocking nits left as-is: N2 (ack uses reference-equality on backend fields — fine today), N3 (stale `UseTerminalClientFactory<T>` docstring in core `ITerminalClientFactory.cs` — pre-existing, out of scope).
- Gates green: `make build` 0/0, `env -u MEKO_API_KEY make test` all pass, `validate --strict` valid.
- Orchestrator hygiene: removed a stray untracked `--list` file (54KB accidental schema-dump redirect) from the repo root before committing.
- **For §3's architect (remaining wiring):** §4 covered 3.1/3.2 for `Daemon.Routing`. What's LEFT in §3: task 3.3 (Makefile `daemon-app` target — needs the §6 Swift package to exist first, so do it with/after §6) and task 3.4 (`daemon/Directory.Build.props` for `Daemon.*` assembly defaults — legit first per-area props; only needed once §5's `Daemon.cs` or more daemon C# projects benefit). §3 may end up folded into §5/§6 rather than a standalone block.

## Section 11 — Daemon.Routing test suite (DONE)

- **11.1–11.5 complete; reviewer APPROVED after two fix rounds.** New `test/Daemon.Routing.Tests/` (22 tests): routing invariants (incl. egress-first precedence + threshold boundary), privacy invariant both directions (distinct `personal_tool`/`world_tool` names), exactly-once counter via `MeterListener`, streaming ack, the terminal-client hook (drives public verbs → `ITerminalClientFactory.Create` → `IsType<TriageRouter>`), and the A1 fail-safe. Added to root `daemon.slnx` + `Everything.slnx`. Gates green (`make build` 0/0; full `make test` verified green twice by the orchestrator; `validate --strict`).
- **CARRIES A §4 PRODUCTION FIX (the tests caught a real bug the §4 review missed):** `TriageRouter`'s A1 fail-safe used `classifyResponse.Result ?? default`, but M.E.AI 10.5.2 `ChatResponse<T>.Result` **throws `JsonException`** on unparseable JSON (it never returns null) — so the fallback was dead code and a bad classification crashed the turn. Fixed to `TryGetResult(out RouteDecision? parsed) && parsed is not null ? parsed : new RouteDecision("personal", Tier.Direct, false, 1f)` (non-throwing). **Lesson:** prefer `TryGetResult` over `Result` for structured output.
- **Spec/design realignment (orchestrator):** the §11 tests also exposed that design.md R5 prose said reasoner dispatches "regardless of scope", contradicting the `triage-routing` spec's egress-first order. The SPEC is authoritative and self-consistent (egress when world+impersonal+confident; **otherwise** reasoner when Tier.Reasoner). Production code was already correct (egress-first); design R5 prose corrected to match; the mis-encoded test was rewritten (`EgressConditionMet_ReasonerTier_RoutesToEgress_NotReasoner`).
- **Flaky-test fix:** `DaemonTelemetry`'s counter is a process-global `Counter<long>`, so a `MeterListener` sees measurements from ALL parallel test classes → counter delta bled to 2 under xUnit's default cross-class parallelism. Fixed with `[assembly: CollectionBehavior(DisableTestParallelization = true)]` in the test assembly; all 5 counter-measuring tests route through one `MeasureCounterDeltaAsync` helper. **Arch note (for whoever extends the telemetry surface):** if counter-correctness tests proliferate, add an injectable recorder / test `IMeterFactory` seam so tests observe an isolated meter instead of the process-global one.
- **Pre-existing flake (NOT introduced here, noted for CI):** the worker hit a one-off `Dmon.Core.Tests` failure in a full `make test` run (591/591 in isolation); it did NOT reproduce in two orchestrator full-suite runs. Latent cross-assembly parallel bleed in the existing Core suite — out of this change's scope, but worth watching in CI.

## Section 5 — Daemon.cs composition root (in progress)

- **Architect verified all §5 prereqs present:** `DmailExtension : IToolExtension` (parameterless ctor, DMAIL_* env) + `AddToolExtension<DmailExtension>()`; `AddDcalAbilities<T>()` (facet verb on `IToolRegistration`); `UseTriage`/`AddReasoner`/`AddEgress` (on `IDmonHostBuilder`, `Dmon.Hosting`); providers `Dmon.Providers.{Ollama,OpenAI,Gemini}`. `IDmonHostBuilder : IProviderRegistration, IToolRegistration, IMiddlewareRegistration` so facet verbs chain on the builder.
- **NOVEL MECHANISM (first repo use of `#:project`):** `daemon/Daemon.cs` is an ADR-019 file-based program, but its first-party deps can't all be `#:package`: `Daemon.Routing` is `IsPackable=false`, and `Dmon.Tools.Dcal`/`Dmon.Tools.Dmail`/`Dmon.Memory` aren't packed by `scripts/pack-core.sh`. So `Daemon.cs` uses `#:project` directives for those four local projects + `#:package dmoncore@0.2.*` + the three provider packages (which ARE on the packed feed). No repo precedent for `#:project` — if it doesn't resolve in this SDK, the worker STOPS and reports 5.1 blocked (does not repackage the local projects). The 5.3 build (`dotnet build daemon/Daemon.cs -c Release`) is the proof.
- **Two orchestrator doc realignments applied (from architect findings):** (1) task 5.3 build command corrected `daemon.slnx` → `dotnet build daemon/Daemon.cs` (a file-based program is not a `.csproj`, not in any `.slnx`). (2) design.md D3: `AddDmonMemory()` extends `IServiceCollection` (called as `builder.Services.AddDmonMemory()`, not chained on the builder); it wires the SHORT-TERM tier (`Dmon.Memory`), with Meko long-term an optional add-on — D3's "Meko long-term tier" comment was imprecise.
- **Deferred to §3/§6 (NOT in §5):** `make daemon-app` / `make build` integration of `Daemon.cs` (task 3.3), and `daemon/Directory.Build.props` (task 3.4 — a file-based program doesn't consume it). §5 is just the file + a hand-run `dotnet build` check.

### §5 outcome — DONE (reviewer APPROVED)

- `daemon/Daemon.cs` created; 5.1–5.3 complete. **Mechanism resolved to all-`#:project` (NOT the hybrid the §5 brief first proposed).** The hybrid (`#:project Daemon.Routing` + `#:package dmoncore@0.2.*`) hit **NU1605**: `Daemon.Routing`→`ProjectReference(Dmon.Core)` (PackageId `dmoncore`, source MinVer 1.0.0) created a transitive `dmoncore >= 1.0.0` that conflicts with the packed `0.2.0`. Fix per **ADR-025 D4** (intra-repo = ProjectReference; `#:package` is for external consumers): `Daemon.cs` references ALL first-party deps via `#:project` (Dmon.Core, Daemon.Routing, Dmon.Tools.Dcal, Dmon.Tools.Dmail, Dmon.Memory, providers Ollama/OpenAI/Gemini) and **zero first-party `#:package`**; external SDKs arrive transitively. No `#:package` at all in the final file. (Orchestrator REJECTED the alternative of tagging `core-0.2.0` — that's a release-strategy change, not a build workaround.)
- Body details: triage verbs return `IDmonHostBuilder` (not `DmonHostBuilder`), so wiring is statement-style on a `DmonHostBuilder builder` (a fluent chain won't compile through `Build()`); `builder.Services.AddDmonMemory()` (memory verb is on `IServiceCollection`); `AddToolExtension(new DmailExtension())` (parameterless ctor reads `DMAIL_*` — behaviour-identical to the generic form); egress uses the **native `GeminiChatClient` (GeminiDotnet)**, not OpenAI-compat (consistent with "Gemini via native SDK"); ADR-005 clean (Gemini key env-only `?? string.Empty`; reasoner `ApiKeyCredential("not-needed")` is a no-auth local placeholder, not a secret).
- Helper-file hygiene: a prior attempt's `daemon/nuget.config`, `daemon/Directory.Packages.props`, and `daemon/Daemon.Routing/Directory.Packages.props` were all REMOVED — the all-`#:project` approach needs none. Final footprint: just `daemon/Daemon.cs`.
- `Daemon.cs` is NOT in any `.slnx` (ADR-019). Gates green: `dotnet build daemon/Daemon.cs -c Release` 0/0, `make build` 0/0, `env -u MEKO_API_KEY make test` all pass, `validate --strict` valid.
- design.md D3 prose updated to document the all-`#:project` rule + the NU1605 reason.

## Section 6 + task 3.3 — Daemon.App Swift scaffold + make daemon-app (DONE)

- **6.1–6.3 + 3.3 complete; reviewer APPROVED (no blockers).** New `daemon/Daemon.App/` SwiftPM package: `Package.swift` (`swift-tools-version: 5.9`, `.macOS(.v14)`, executable product/target `DaemonApp`, no external deps) + `Sources/DaemonApp/DaemonApp.swift` (`@main struct DaemonApp: App` with `MenuBarExtra` + `Settings` STUB scenes only; a Quit button is the sole behaviour). Root `Makefile` gained a STANDALONE `daemon-app` target (`swift build -c release --package-path daemon/Daemon.App`) added to `.PHONY` but NOT to `build:`/`all:` — `make build` stays C#-only (verified it doesn't invoke swift). `.gitignore` ignores `daemon/Daemon.App/.build/`.
- **Swift toolchain:** 6.3.2 installed (`/usr/bin/swift`, `xcodebuild` present). The package is OUTSIDE every `.slnx` (ADR-028 D5).
- **KNOWN false positive (note for IDE users / §7–10 workers):** SourceKit-LSP flags `'main' attribute cannot be used in a module that contains top-level code` on `DaemonApp.swift`. It's a SourceKit heuristic bug for single-file executable targets — the real compiler builds fine (clean `rm -rf .build && swift build -c release` → "Build complete!"). The file is correctly named `DaemonApp.swift` (NOT `main.swift`), so `@main` is valid. Ignore the LSP diagnostic; trust `swift build`.
- Gates green: clean `swift build -c release` 0/0, `make daemon-app` ok, `make build` clean (no swift), `env -u MEKO_API_KEY make test` 1013 passed, `validate --strict` valid.

## Sections 7–10 — Swift menu bar app logic (next)

- Remaining Swift: §7 GatewayManager (Foundation.Process lifecycle + back-off + PID re-adoption), §8 TailscaleMonitor (`tailscale status --json` poll), §9 MenuBarView + status icon, §10 SettingsView (config.yaml + Keychain + SMAppService login item). NO `make test` coverage — gate is `swift build`/`make daemon-app` + manual reasoning. Also still open: task 3.4 (`daemon/Directory.Build.props` — only if a daemon C# project needs `Daemon.*` assembly defaults; `Daemon.Routing` builds fine without it, so 3.4 may be N/A — the next architect should assess).

## Sections 7 + 8 — GatewayManager + TailscaleMonitor (DONE)

- **7.1–7.4 + 8.1–8.3 complete; reviewer APPROVED (no blockers).** Two `@MainActor final class … ObservableObject` files under `daemon/Daemon.App/Sources/DaemonApp/`: `GatewayManager.swift` (Foundation.Process lifecycle: `start`/`stop`/`restart`, `--agent daemon/Daemon.cs`, path order override→`DMON_GATEWAY_PATH`→`~/.dotnet/tools/Dmon.Gateway` with `~` expanded, back-off `min(×2,60)` reset-to-2-on-success, `intentionalStop` flag so `stop()` doesn't restart, PID re-adoption at `~/.dmon/run/gateway.pid` via `kill(pid,0)`) and `TailscaleMonitor.swift` (`enum TailscaleStatus{up,degraded,down}`, 30s `tailscale status --json` poll on a detached background Task, publish via `MainActor.run`). No views, no `Package.swift`/`DaemonApp.swift` changes.
- **Tailscale decision table (spec-authoritative, encoded literally):** binary-not-found→`.degraded`; non-zero/unparseable/`BackendState!="Running"`→`.down`; Running+no-peers→`.degraded`; Running+peers→`.up`. (The spec's missing-CLI=degraded overrides D5's "red".)
- **Note for §9 (reviewer's architectural flag):** an *adopted* (non-child) Gateway has no `terminationHandler`, so a crash of an adopted process won't trigger back-off restart until the next app launch (inherent Foundation limitation; consistent with the OQ-B PID-file design — the `/health`-poll alternative was deferred). §9 may add a supervisory poll to cover the adopted-crash gap; not required by §7/§8.
- Non-blocking nits left as-is: `SelfNode`/`PeerNode` decode structs (the `Self` field is decoded but the table keys on peers — harmless; §9 may use it); `lastExitCode` published for §9's view.
- Gates green: clean `swift build -c release` 0/0, `make build` clean (no swift), `env -u MEKO_API_KEY make test` all pass, `validate --strict` valid.

## Section 9 — Live MenuBarView + status icon + service wiring (DONE)

- **9.1–9.3 complete; reviewer APPROVED (no blockers).** `MenuBarView.swift` (3 status rows: Gateway/Tailscale/last-sync; buttons Start-Stop Gateway, Sync Calendar Now, Open Settings via `SettingsLink`, Quit), `DaemonApp.swift` rewritten (`@StateObject` for `gateway`/`tailscale`/`health`, `iconColor` computed from the spec table, `MenuBarExtra(isInserted:)` trailing-closure label tinted via `.foregroundStyle`, `.environmentObject(...)` + `.task { start() }`), and a new `DcalHealthMonitor.swift` (mirrors TailscaleMonitor; 30s `/health` poll → `@Published lastSync: String?`; `syncNow()` POSTs `/api/sync`; fail-soft — server down ⇒ nil, never crashes). `GatewayManager`/`TailscaleMonitor`/`Package.swift` UNCHANGED; Settings scene still a stub (§10 owns it).
- **Combined-status icon table (daemon-host spec, exact):** `guard gateway.isRunning else { return .red }` (stopped⇒red precedence); running+`.up`⇒`.green`; running+`.degraded`/`.down`⇒`.orange` (amber). All three spec scenarios satisfied.
- **Server contract cross-checked:** `services/Dcal/Program.cs` `GET /health` → `{lastSync, eventCount}` (camelCase, nullable), `POST /api/sync` → 204, auth header `X-Api-Key`. `DCAL_BASE_URL` default `http://localhost:5280`.
- **KNOWN SourceKit false positives (note for IDE users / §10 worker):** SourceKit-LSP now also flags `Cannot find type 'GatewayManager'/…/'MenuBarView' in scope` across the files (on top of the `@main` one). These are LSP multi-file-target indexing bugs — a clean `rm -rf .build && swift build -c release` compiles+links all 5 files ("Build complete!"). In a single SwiftPM target, sibling-file types are the same module (no import). IGNORE the LSP; trust `swift build`.
- **Nits for §10 to optionally tidy:** `DcalHealthMonitor.eventCount` is published but unused by any view; redundant `?? nil`; dead `_ = response` in `postSync`; `fetchHealth()` decodes on the main actor (TailscaleMonitor's `poll()` is `static`/off-actor) — fine (async, tiny payload), but a `static` fetch would push decode off-main for strict parity. None block §9.
- Gates green: clean `swift build -c release` 0/0, `make build` clean (no swift), `env -u MEKO_API_KEY make test` all pass, `validate --strict` valid.
