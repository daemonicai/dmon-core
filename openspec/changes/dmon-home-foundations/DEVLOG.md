# DEVLOG: dmon-home-foundations

<!-- PRD Phases 0+1 of home/PRD.md: the dmon-home macOS host — foundations, supervision, and the text loop. -->

## 1. ADR-037 and the `home/` bucket

- **[architect]** Base: `ac23f64` — ADR-037 accepted and indexed; the `home/` bucket established as a first-class monorepo bucket.
- **[architect]** Pre-flight findings on task 1.2, both corrected in-flight rather than escalated:
  - `CLAUDE.md` has **no ADR table** — commit `ac23f64` ("docs(claude): move ADR index and apply loop to lazy skills", #108) moved it into the `adr-index` skill. 1.2 as written targets a table that no longer exists. Corrected scope: the row goes in `.claude/skills/adr-index/SKILL.md`; `CLAUDE.md` and the skill's frontmatter carry a **count** ("all 35 accepted ADRs") which is what needs touching there.
  - That count is already wrong and the index has a gap. 35 is the **file** count; four are not accepted (ADR-009 superseded; ADR-017/030/031 Proposed). `adr-index` lists 31 rows and is **missing ADR-036** (Accepted, landed with the defense-in-depth batch). Decision: add the missing ADR-036 row as part of this block. Rationale — 1.2 requires stating a count in a file we are editing, and writing a knowingly-understated one is worse than the one-line fix. Flagged to the Product Owner as a deliberate in-scope correction, reversible on request.
- **[architect]** Block 1.1–1.3: write ADR-037, index it, establish the bucket. Single block — the three tasks are one coherent deliverable (an ADR is not indexable before it exists, and the bucket is what the ADR sanctions), all documentation and scaffolding, no code. Gates pass trivially; the risk here is *content* accuracy, not build breakage, so the reviewer's attention goes to whether ADR-037's amendment claims against ADR-025/ADR-028 are precisely stated.
- **[architect]** Decision: `home/PRD.md` is committed with this block. It is the requirements source for the whole change and was sitting untracked; the bucket materialising with its first member is exactly ADR-025's rule. Pre-existing untracked files unrelated to this change (`AUDIT*.md`, `PERSONAL_DAEMONS.md`, `sandbox-*/`) are **not** staged.

- **[worker]** 1.1–1.3 done. `docs/adrs/ADR-037-home-bucket-and-dmon-home.md` written (Accepted, 2026-08-02, D1 bucket / D2 gateway client / D3 dmonium supersession + naming / D4 speech sidecar). `adr-index` gained rows for ADR-036 and ADR-037; count sentences updated in `CLAUDE.md` and the skill frontmatter. 1.3 verified: `home/` holds only `PRD.md`, no `home.slnx`, and none of the eight root `.slnx` files reference `home/`. Gates green — `make build` clean (0 warnings), `env -u MEKO_API_KEY make test` all suites passing (Dmon.Core 613/1 skipped, Dmon.Network 222, Dmon.Terminal 187, no failures), `openspec validate --strict` valid.
- **[architect]** Mid-block correction after Product Owner input on the split-LAN topology (see NEXT): stopped the worker before gates, corrected design.md D2, and had it re-scope ADR-037 D2 from "loopback WebSocket" to a **configured gateway endpoint**, with the PRD §2.1 stdio fallback marked contingent on co-location. D1/D3/D4 untouched. Recorded because the near-miss is the lesson: the narrowing was mine, not the PRD's — PRD §7.4 already said "reached by hostname, not assumed to be local", and a binding ADR nearly shipped asserting the opposite.
- **[architect]** Pre-review defect, sent back to the worker rather than spending a review round: the count written to `CLAUDE.md` and the `adr-index` frontmatter said **"33 accepted ADRs"**, derived from the index row count. Rows are 33 but accepted ADRs are **32** — the extra row is **ADR-009**, `Superseded by ADR-019`, which belongs in the index but is not accepted. (ADR-017/030/031 are `Proposed` and correctly absent; ADR-013's file status is `Accepted` despite the index noting ADR-022 superseded it, and the file is source of truth.) Fix is 33 → 32 in those two sentences only.

- **[reviewer]** Request changes on 1.1–1.3. **Blocker:** the new ADR-037 row in `.claude/skills/adr-index/SKILL.md` says the host speaks `gw` frames *"over loopback WebSocket"* — reintroducing the exact framing the mid-block correction removed, and contradicting ADR-037 D2 ("configured gateway endpoint… configuration, not a hard-coded assumption"). Sharp call on *why* it matters most there: the index exists to be trusted **without** opening the source ADR, so a reader consulting only the row would conclude a future split topology contradicts a binding decision — the opposite of what D2 says. **Nit:** the `Amends:` header (line 5) marks ADR-025 D10 prospective but not ADR-028 D6, though Decision 3 and the Relationship section treat both as prospective. Verified clean: amendment scoping against the actual ADR-025/ADR-028 decision text; amends-not-supersedes (every "supersed" refers to the dmonium *product*, never to an ADR); no assertion of dmonium removal, with `daemon/Daemon.App`, `ci.yml` and `release.yml` untouched; the ADR-036 row factually matching its source; the 32-count recomputed independently from every file's `**Status**` line; and all eight root `.slnx` files checked directly for `home/` references.
- **[architect]** Both sent to the worker. Noted for the record: the blocker is one my own review missed — I corrected the ADR body and did not re-read the index row I had asked to be written from the *pre-correction* brief. The lesson generalises past this block: when a decision's wording changes mid-flight, every artifact **derived** from it needs re-checking, not just the source.
- **[architect]** Carried forward, out of scope (reviewer's architectural note): `adr-index` row for **ADR-013** is annotated *"(Superseded by ADR-022.)"* but `docs/adrs/ADR-013-agent-profiles.md` still reads `**Status:** Accepted` with no supersession note — unlike the ADR-009 pair, where row and file agree. Pre-existing, untouched by this block, and it does not affect the 32-count (which was recomputed from file statuses). Logged in NEXT for a future docs pass.

- **[worker]** Both fixed. Index row now reads *"over a **configured** WebSocket gateway endpoint (loopback in this change's co-located deployment)"*; line 5 `Amends:` now carries "— prospectively" on ADR-028 D6 to match ADR-025 D10. ADR body untouched. Gates green. → @reviewer
- **[reviewer]** **Approve.** Verified both fixes against the working tree rather than the report. Index row now matches ADR-037 D2's own wording and no longer asserts loopback as a property of the decision; house style, bolding and the closing amends/builds-on clause intact; "supersedes `dmonium`" correctly scopes to the *product*, not to an ADR. Line 5 now internally consistent with Decision 3 and the Relationship section. Confirmed nothing else moved: whole tracked diff is 4 insertions / 2 deletions, the ADR-036 row is byte-identical to the prior pass, and the 32-count is unchanged in both files.
- **[architect]** Gates green, boxes 1.1–1.3 ticked, block committed. Staged only this block's files — `docs/adrs/ADR-037-home-bucket-and-dmon-home.md`, `.claude/skills/adr-index/SKILL.md`, `CLAUDE.md`, `home/PRD.md`, and the change directory. Pre-existing untracked files (`AUDIT*.md`, `PERSONAL_DAEMONS.md`, `sandbox-*/`) deliberately left unstaged.

- **[supervisor]** Section review of `ac23f64..HEAD` (`a3c48a3`, tasks 1.1–1.3). **Request changes** — three blockers, all documentation-level. Engineering judgement sound: the asymmetric supersession holds everywhere; ADR-037 states parity *more* strictly than design D9, so it cannot later be quoted loosely; `daemon/`, `ci.yml`, `release.yml` and the `.slnx` set genuinely untouched.
  1. **The mid-flight D2 correction never reached `proposal.md`** (`:40`, `:42-43`) — still said "over loopback WebSocket" and "swapped for a direct stdio core", contradicting binding ADR-037 D2 and design.md D2. Third artifact carrying the pre-correction framing; the proposal is the one a future reader opens first and the one that survives into the archive.
  2. **The `monorepo-layout` delta asserted an artifact the ADR explicitly defers** — `home/` described as "an app artifact, independently versioned", while ADR-037 marks D10/D6 amended *prospectively* and says `dmon-home` "has none yet, by design". Archiving as written would have put the standing spec set in contradiction with itself and licensed a `dmon-home` release artifact. Mechanism identified: the clause was copied from the adjacent `services/` parenthetical, so it reads as house-consistent phrasing to a diff-local review.
  3. **Nothing in the section accounted for `home/PRD.md`** — ADR-037 D1, `specs/dmon-home/spec.md:5` and the `monorepo-layout` scenario all enumerate the bucket's contents without admitting the one file actually in it, while invoking the "memberless role bucket has no directory" rule in the same breath. Also flagged 1.3's satisfies-annotation as over-claiming: the `dmon-home` bucket requirement completes at 2.1–2.2, not here.
- **[architect]** Remediation applied directly — all three are doc-only spec/design realignments, which the apply workflow assigns to the Architect rather than a worker. No new task numbers, no boxes ticked. Changes: `proposal.md` GatewayClient bullet reworded to "configured WebSocket gateway endpoint" with the stdio fallback marked contingent on co-location, and its Impact section corrected to include the `Power` package it had omitted; `specs/monorepo-layout/spec.md:5` now says `home/` ships **no release artifact** and joins the app-artifact family only if and when it gains one; ADR-037 D1 and `specs/dmon-home/spec.md:5` now list the requirements document as bucket content, with D1 noting the bucket materialises with it as its first member; `tasks.md` 1.3 annotation re-scoped to the `monorepo-layout` scenarios with an explicit note that the `dmon-home` requirement completes at 2.1–2.2. Also took supervisor notes 4 and 7: `in this change's co-located deployment` → `in the co-located deployment` throughout ADR-037 and the index row (a binding document must not point at a change directory that archives away), and `design.md` speech-sidecar cross-reference corrected from D4 to D7. `openspec validate --strict` passes; a sweep for the pre-correction framing across every change artifact, the ADR and the index now returns only the DEVLOG's own quotations of the defect.

- **[supervisor]** Re-reviewed `ac23f64..HEAD` (`a3c48a3` + `ca43ad4`). **Approve.** All three blockers verified resolved against the tree: proposal.md now matches ADR-037 D2 and design D2 in substance *and* emphasis; the `monorepo-layout` delta agrees with the ADR's prospective framing, design D10 and the standing `package-publishing` app-artifact family (which correctly still has no `home/` member); and four artifacts now tell one story about `home/PRD.md`, with `tasks.md` stating plainly that the bucket requirement completes at 2.1–2.2. No over-correction: both spec deltas are one changed line each with every `#### Scenario:` header byte-identical to `a3c48a3` (so archive header-matching is safe), and the remediation diff is 36 insertions / 18 deletions across eight files, all documentation, touching no code, workflow, `Makefile` or `.slnx`. Confirmed the disposition of all six parked notes.
- **[supervisor]** One further instance of the same drift class, named rather than blocked: `specs/dmon-home-gateway-client/spec.md:15` was the last artifact presenting the stdio swap as *the reason* the transport abstraction exists. Suggested folding the reword into task 6.1 rather than opening another remediation round.
- **[architect]** Fixed it now rather than deferring to 6.1, departing from the supervisor's suggested timing on purpose. Its reasoning was process cost, not correctness — but the lesson of this whole section is that **deferred sweeps get missed**: this one defect has been chased through `design.md`, `ADR-037`, the `adr-index` row and `proposal.md`, each time because a sweep stopped short. Carrying a known-stale rationale five sections forward into a delta that syncs into a **standing** spec at archive is precisely the failure we have now paid for twice. The requirement clause was already correct; only the trailing rationale changed, to lead with the in-memory-conformer testability payoff and generalise "a direct stdio core" to "an alternative transport". Still flagged in the section-6 carry-forward as belt-and-braces.
- **[architect]** **Section 1 closed.** Blocks: 1 (tasks 1.1–1.3) plus one remediation block. Commits: `a3c48a3`, `ca43ad4`, and the rationale reword above. All boxes ticked, supervisor approved.

## 2. Xcode project and Swift package skeleton

- **[architect]** Base: `7737035` — a generated Xcode project, the `dmon-home` Swift packages, and `make` targets that build and test them headlessly.
- **[architect]** Block 2.1–2.3 — the whole section as one block, revising the two-block carve I sketched in NEXT. Reason: gates are `make build` / `make test` / `openspec validate`, all of which are .NET-side and would pass *trivially* for 2.1–2.2 while proving nothing about the Swift code. The `make` targets in 2.3 are what make this section verifiable at all, so splitting them off would produce a block whose gates are vacuous. One block, one commit.
- **[architect]** Decisions taken for this block, all recorded here because none is in `design.md` yet:
  - **Swift 6 language mode**, not dmonium's 5.9. Toolchain on this machine is Swift 6.3.3 / Xcode 26.6. PRD §4.3 permits `@unchecked Sendable` and `nonisolated(unsafe)` *"at the ring buffer boundary, where a safety property is being asserted that the compiler cannot see"* and warns that their appearance elsewhere means "a concurrency diagnostic has been suppressed rather than a problem solved" — a constraint that is only meaningful under strict concurrency checking. Choosing 5.9 now would silently void PRD §4.3 before the audio phases start.
  - **One `home/Package.swift` with three library targets**, not three separate packages. PRD §2.3's table says "packages"; targets give the same module seam and import boundary, with one manifest to maintain and a single `swift test --package-path home` that mirrors the existing `daemon-app-test` pattern. XcodeGen wires one local package rather than three.
  - **`make dmon-home` / `make dmon-home-test`**, named for the product, mirroring `daemon-app` / `daemon-app-test`. Must not disturb the existing pair (design D9 / spec "The existing Swift app is untouched").
  - **The generated `.xcodeproj` is gitignored**; `home/project.yml` is the source of truth. `.gitignore:90` already ignores `daemon/Daemon.App/.build/`, so the `home/` equivalents go alongside it.
  - **Seeded, not stubbed.** `specs/dmon-home/spec.md` forbids a package existing "solely as a placeholder for a later phase", and SPM requires every target to have at least one source file. Resolution: each target is seeded with a small type its own section genuinely needs and will not delete — `Power.ActivityAssertion` (used by 5.2), `Supervisor.ChildID` + `ChildHealth` (used by 4.1/4.3), `GatewayClient.WireVersion` (used by 6.4). The "no empty placeholder packages" scenario is judged at **change** scope, not block scope: Supervisor, GatewayClient and Power all gain substantial code in sections 4, 5 and 6 of *this* change, which is exactly what distinguishes them from `AudioEngine`/`Speech`/`Directedness` (design D3) — those belong to a later change and are not created.

- **[worker]** 2.1–2.3 done. `home/project.yml` (XcodeGen, `DmonHomeApp`, `ai.daemonic.dmon-home`, macOS 14+), `home/Package.swift` (`swift-tools-version: 6.0`, `.swiftLanguageMode(.v6)` on all six targets), three library targets under `home/Sources/` with tests under `home/Tests/`, app shell at `home/App/DmonHomeApp/` (outside the package's source trees). `make dmon-home` / `make dmon-home-test` added after `daemon-app-test` and to `.PHONY`. `.gitignore` gained `home/.build/` and `home/*.xcodeproj`. Seeds: `Power.ActivityAssertion`, `Supervisor.ChildID`/`ChildHealth`, `GatewayClient.WireVersion` (`current = 0.2`). Gates: `make build` 0 warnings, `env -u MEKO_API_KEY make test` green, `make dmon-home-test` 17/17 headless, `make daemon-app-test` 72/72, `openspec validate --strict` valid. → @reviewer
- **[worker]** Self-caught against brief hazard 3: `ActivityAssertion` was first drafted with `NSLock` + `@unchecked Sendable`, then rewritten as an `actor`. No unsafe concurrency escape hatches anywhere in the block.
- **[architect]** Verified the worker's claims independently before review rather than taking the report: re-ran `make dmon-home-test` (17/17 confirmed), confirmed the generated `.xcodeproj` does not appear in `git status`, and confirmed nothing outside the block moved. The editor's SourceKit diagnostics ("No such module 'Testing'/'GatewayClient'") are **index noise, not build failures** — `Testing` ships with the Swift 6.3.3 toolchain and resolves at build time; told the reviewer not to report them.
- **[architect]** Worker also hit the known dmonium gotcha: `swift test` deadlocking on a stale `.build` lock, ~5 min hang at near-zero CPU on `mach_msg`. Pre-existing (recorded from the `dmonium-mlx-realignment` change), unrelated to this block, resolved by killing the stale PID.
- **[architect]** **Unbriefed divergence sent to the reviewer for a verdict:** the worker used **swift-testing** (`import Testing`, `@Test`) for all `home/` tests, while `daemon/Daemon.App` uses **XCTest**. It works (17/17), and greenfield + modern-default + better ergonomics for value-type tests all argue for it, against the real cost of two frameworks in one repo for anyone moving between the Swift trees. Not waving it through on my own judgement — asked for an explicit recommendation so it lands as a **recorded decision** rather than an implicit one, which is how the D2 drift started.

- **[reviewer]** **Approve.** Verified rather than inferred: deleted `home/DmonHomeApp.xcodeproj`, ran `xcodegen generate`, rebuilt via `xcodebuild` → BUILD SUCCEEDED, proving the regeneration scenario for real. 17/17 Swift tests, zero build warnings, dmonium 72/72 unaffected, `daemon/` zero working-tree diff, no `home.slnx`, no `.slnx` referencing `home/`. Grepped all of `home/` for `@unchecked Sendable` / `nonisolated(unsafe)` / `@preconcurrency` — none; `.swiftLanguageMode(.v6)` confirmed on all **six** targets, and `project.yml`'s `SWIFT_STRICT_CONCURRENCY: complete` is redundant-not-contradictory. No sandbox/entitlement/microphone strings anywhere — section 3's scope correctly untouched.
  - `ActivityAssertion` correctness confirmed at the code, not the test: `begin()` guards `token == nil` so a second call is a true no-op (no leaked handle), and `release()` nils the token **before** calling `endActivity`, so re-entry cannot double-release a stale token. Actor isolation proves the serialisation the doc comment claims.
  - `WireVersion` correctness confirmed: compatibility checks major **and** minor (not the common major-only bug), `current` is `0.2`, and parsing throws on malformed input rather than defaulting.
  - **Directed check on swift-testing: recommend accept, and record it explicitly.** Reasoning accepted in full — the two-idiom cost is real but *bounded*, because ADR-037 D3 retires `daemon/Daemon.App` at parity so the trees are not meant to coexist; swift-testing's async-native `#expect` fits `actor`-isolated state materially better than XCTest plus boilerplate; and nothing in `home/` couples to XCTest-only machinery that would force a mixed approach later.
  - **Nit (not fixed):** `home/App/DmonHomeApp/ContentView.swift` constructs a `placeholderActivityAssertion` it never begins or releases. Harmless — actor init is side-effect-free and no resource is held — but it is the one place the app shell instantiates something with real semantics purely to prove linkage. Left as-is; **raised explicitly with the section-2 supervisor** rather than waved through, since "instantiated but unused" is squarely in the dead-scaffolding family a section review hunts, and 5.2 is the task that wires this type for real.
- **[architect]** Recorded D14 in `design.md` — Swift 6 language mode, the single-manifest/three-target layout, and swift-testing — rather than leaving them in the DEVLOG only. Decisions that live solely in a working log are exactly how the section-1 D2 drift started: `design.md` is what later sections and the supervisor read. Boxes 2.1–2.3 ticked.

- **[supervisor]** Section review of `7737035..HEAD` (`4022192`, tasks 2.1–2.3). **Request changes** — two blockers, both small, one remediation block. Substance sound: no ADR erosion, no artifact/code drift beyond the one noted, no generated or machine-specific leakage (commit is exactly 17 files; `.build`, `.xcodeproj` and `.swiftpm` all ignored; no `Package.resolved` because there are no external dependencies).
  1. **Nothing tracked compiles the app target.** `make dmon-home` builds only `home/Sources/*`; `home/App/DmonHomeApp/` is outside `Package.swift`'s target paths, so both app source files are compiled by **no** `make` target. `xcodegen`/`xcodebuild` appear nowhere in the `Makefile`, `.github/`, or any tracked instruction — only as prose in `home/PRD.md:301`. Four consequences in ascending cost: a fresh clone cannot build the app; the "regenerates from the manifest" scenario is satisfied by tribal knowledge rather than a command; **task 3.3's recipe has no referent** — `make dmon-home` produces no `.app`, and 3.3 is the one task needing the Product Owner's hands; and section 10.1's CI would leave `ContentView.swift` free to break with every gate green.
  2. **`design.md` D3 contradicted D14.** D3 said "four SPM packages"; what shipped is one package with three library targets plus the app target. D14 declared the substitution but D3 was never reconciled — the same cross-artifact drift class that cost section 1 two rounds, at smaller scale.
- **[supervisor]** Ruling on `ContentView.swift`'s `placeholderActivityAssertion`: **remove it.** Three reasons, the third only visible at section scope. (a) It proves *less* than its neighbours — `wireVersion` and `placeholderChildHealth` are read in `body`, so the compiler proves those modules link; an unused stored property emits no Swift diagnostic and proves nothing. (b) **It seeds the wrong ownership for 5.2** — an `actor` stored on a SwiftUI `View` struct, which SwiftUI recreates on invalidation; the obvious next move is `.task { await …begin() }`, binding the assertion to *view* lifetime when the spec requires *gateway* lifetime. Not inert: it models the wrong owner. (c) Its justification is void while nothing repeatable compiles the app. Overturns the reviewer's "not worth a fix", which was defensible on the diff alone — vindicating passing it up rather than accepting it.
- **[architect]** Doc half of the remediation done directly: `design.md` D3 reworded to "four modules — three library targets in one package, plus the app target" with a D14 cross-reference; D14 gained a paragraph stating the swift-testing decision does **not** depend on the retirement schedule (supervisor note 3 — the two trees share no test code or fixtures, so the cost is a reading cost, not a maintenance one; the bounded-window argument explains why the cost is small, not why the decision is right); `tasks.md` 3.3 now names `make dmon-home-app` and records that ad-hoc signing makes a fresh TCC prompt expected on every rebuild, so `tccutil reset` should be unnecessary. Makefile target and the `ContentView` removal briefed to the worker.
- **[architect]** My miss on blocker 1, worth naming: I verified `make dmon-home-test` passed and never asked *what compiles the app*. Gate-passes-green is not the same question as everything-is-gated.

- **[worker]** Remediation done. `make dmon-home-app` added (`.PHONY` + target), with a comment recording the `xcodegen` prerequisite at the point of use. Two corrections to my draft, both right: `--project home` pins xcodegen's output directory deterministically rather than relying on unstated default behaviour, and `-configuration Release` matches what `dmon-home` already does. Scheme name verified via `xcodebuild -list` rather than assumed. `placeholderActivityAssertion` and `import Power` removed from `ContentView.swift`; all three imports in `DmonHomeApp.swift` confirmed genuinely dead and removed; `wireVersion` and `placeholderChildHealth` retained and still read in `body`, so `GatewayClient`/`Supervisor` linkage is still proven.
- **[architect]** Follow-on I sent back before review, from the worker's own report rather than a gate failure: the built `.app` was landing in `~/Library/Developer/Xcode/DerivedData/DmonHomeApp-<hash>/…`, where the hash derives from the checkout's absolute path. Task 3.3 requires the **Product Owner** to launch that bundle from Finder by hand — the one task in this change needing a human — and PRD §1 says to take the lower-friction option where a choice exists. Now pinned via `-derivedDataPath home/.build-xcode`, giving a stable `home/.build-xcode/Build/Products/Release/DmonHomeApp.app`, verified identical after deleting the directory and rebuilding cold. Kept distinct from SwiftPM's `home/.build/` so the two build systems cannot collide; `.gitignore` updated and confirmed via `git check-ignore -v`. Task 3.3's recipe now names the path with an `open -R` to reveal it.
- **[architect]** Verified the app build myself (`make dmon-home-app`, exit 0) rather than trusting the report. The editor's new SourceKit diagnostics — including *"'main' attribute cannot be used in a module that contains top-level code"* — are index noise again, the third such false alarm on this tree; the real `xcodebuild` is clean.
- **[architect]** ⚠️ **Pre-existing flaky test, recorded not dismissed.** The worker hit `WizardEngineTests.InvalidChooseOneAnswer_RePromptsStep` (`test/Dmon.Core.Tests/Rpc/WizardEngineTests.cs`, covering `core/Dmon.Core/Rpc/ProviderSetupHandler.cs`) failing on one `make test` run and passing standalone and on rerun. **Not caused by this block** — its file scope is `Makefile`, `.gitignore` and two Swift files, none of which the .NET test suite touches. Logging it because "passed on rerun" is how a real intermittent bug gets buried: a wizard step that intermittently fails to re-prompt on invalid input is a plausible ordering or async-timing defect, not obviously test-only. Carried to NEXT for a separate investigation; **not** in scope for this change.

- **[reviewer]** **Approve** on the remediation diff — no blockers, no nits. Checked `-quiet` against `xcodebuild -help` to confirm it suppresses only non-warning/error output and still propagates a nonzero exit, so a real failure still fails `make`. Independently assessed `-derivedDataPath` against the alternatives and endorsed it: standard single-flag pinning, repo-relative, and it cannot contend with Xcode.app because a GUI build uses its own default DerivedData. Noted `--project home` is technically redundant (xcodegen already defaults to the spec's directory) but harmless and defensibly explicit. Confirmed `daemon-app`, `daemon-app-test`, `dmon-home`, `dmon-home-test` and `network` byte-identical to `4022192`; `wireVersion` and `placeholderChildHealth` still interpolated in `body` so the linkage proof survives; the three `DmonHomeApp.swift` imports were dead even before the fix; and `Power`'s D3 standing rests on its own four passing tests, untouched by the removal.

- **[supervisor]** Re-reviewed `7737035..HEAD` (`4022192` + `a9c106a`). **Approve.** Both blockers verified closed against the tree. All four consequences of blocker 1 are shut: a fresh clone has a command, the "regenerates from the manifest" scenario is executable rather than remembered, `tasks.md` 3.3 names a real path, and section 10 has something that compiles `ContentView.swift`. `design.md` `## Decisions` now tells one story, and the `Power` paragraph is untouched and still correct because it never rested on `ContentView`. Endorsed both worker corrections as load-bearing: xcodegen's default project directory resolves relative to the spec and would have shifted silently if the spec ever moved, and XcodeGen's scheme names are configurable rather than equal to the target name by construction, so verifying via `xcodebuild -list` was the right instinct.
  - On the derived-data pin: *"I identified that the app was unbuildable; you identified that buildable isn't sufficient when a human executes the recipe by hand. A DerivedData path hashed from the checkout's absolute location is unquotable in a task file and unverifiable by anyone but the person whose machine produced it — for the one task in this change that needs the Product Owner's hands, that is the difference between a recipe and a hint."*
  - Confirmed the new target cannot mask a failure: `-quiet` still prints warnings and errors and still exits non-zero; each recipe line is its own shell with no `-` prefix, no `|| true` and **no pipe** — a pipeline would have been the real hazard, since the exit status would come from the tail. A failing `xcodegen` aborts before `xcodebuild` runs.
  - Regressions clean: `daemon-app`, `daemon-app-test`, `network`, `release-wave`, `dmon-home`, `dmon-home-test` all byte-identical; no `ci.yml`/`release.yml`/`daemon/` file in range; `home/`'s tracked file list unchanged at 13; `home/.build-xcode/` directory-anchored so it cannot swallow a source path.
  - Noted the follow-through the worker did unasked: `ContentView.swift`'s doc comment was updated to say it wires up `Supervisor` and `GatewayClient` rather than left claiming `Power`. That is precisely this defect class — code removed, prose left asserting the old shape — caught without being told.
- **[architect]** **Section 2 closed.** Blocks: 1 (tasks 2.1–2.3) plus one remediation block. Commits `4022192`, `a9c106a`. All boxes ticked, supervisor approved.

## 3. App bundle, entitlements, and the microphone gate

- **[architect]** Base: `0a593d0` — a real unsandboxed `.app` carrying the microphone usage description, an authorisation-request path with a status surface, and the Product Owner's manual verification that the TCC prompt actually appears.
- **[architect]** Block 3.1–3.2 to the worker. **3.3 is deliberately not in the block** — it is not worker-shaped work at all. It is the Product Owner launching the built bundle from Finder and reporting what macOS shows. No agent can substitute for it, and per `CLAUDE.md` a task requiring human-in-the-loop verification is not tickable without their confirmation.
- **[architect]** Placement call for 3.2's authorisation code: **the app target, not a package.** TCC authorisation is an application-level concern — the grant is keyed to the bundle and surfaced in app UI — whereas PRD §2.3 scopes `AudioEngine` to Core Audio I/O, route management, VAD, playback and barge-in, none of which this is. Creating `AudioEngine` now for a permission query would also put a package in the tree three sections before the change that fills it (design D3). Keeping it minimal in the shell does not foreclose moving it when `AudioEngine` lands. Flagged to the reviewer as a judgment call to assess rather than presented as settled.
- **[architect]** Gate design worth recording: 3.1's two scenarios are **automatable even though 3.3 is not.** The built bundle can be inspected directly — `plutil -p` for `NSMicrophoneUsageDescription`, `codesign -d --entitlements` for the absence of the App Sandbox entitlement. That converts "the bundle is configured correctly" from a human judgement into a gate, leaving the human to verify only the thing a human must: that macOS actually presents the prompt.

- **[worker]** 3.1–3.2 done. `INFOPLIST_KEY_NSMicrophoneUsageDescription` added as one line in `project.yml`'s `settings.base`, keeping `GENERATE_INFOPLIST_FILE` as the single source of truth. New `home/App/DmonHomeApp/MicrophoneAuthorizationModel.swift` — a `@MainActor @Observable` wrapper over `AVCaptureDevice.authorizationStatus(for:)` / `requestAccess(for:)`, with label and guidance covering all four states distinctly. `ContentView` gains a status line, guidance caption and an explicitly-triggered request button. Nothing requests at launch. Gates green; 3.3 not attempted, per brief.
- **[worker]** Swift 6 strict concurrency rejected the un-isolated `@Observable` class being mutated from a `Task` inside a `View`; resolved by isolating the type with `@MainActor` — no suppression, no escape hatch. Second time the language mode has caught a real isolation question in this tree (the first was `NSLock` + `@unchecked Sendable` in `ActivityAssertion`), which is the D14 rationale paying out twice in three sections.
- **[architect]** Verified 3.1's two scenarios myself against the **built bundle** rather than the report: `plutil -p` shows `NSMicrophoneUsageDescription => "dmon-home uses the microphone so you can talk to your daemon by voice."`, and `codesign -d --entitlements -` shows only `com.apple.security.get-task-allow` with **no** `com.apple.security.app-sandbox` key at all. Both scenarios are evidenced, not asserted — which was the point of designing them as inspectable gates.
- **[reviewer]** **Approve**, no blockers. Verified both worker claims at the code: `init()` calls only the synchronous `authorizationStatus(for:)` which never prompts, and the sole `requestAccess()` call site is the `Button` action — no `onAppear`, no `.task`, no lifecycle modifier anywhere in the app tree. Both `switch`es exhaust all four states with distinct copy and use `@unknown default:` (forward-compat) rather than `default:` collapsing a known case. Confirmed the `@MainActor` fix is genuine: grepped the app target for `unchecked` / `nonisolated` / `preconcurrency` / `assumeIsolated` — no matches. `project.yml` still single-source; no entitlements file, no `CODE_SIGN_ENTITLEMENTS`, no `INFOPLIST_FILE`. Makefile flags untouched, so 3.3's hard-coded path stays valid.
  - **Concurs with the app-target placement** (directed check 3): `MicrophoneAuthorizationModel` touches none of PRD §2.3's `AudioEngine` scope (Core Audio I/O, routing, VAD, playback, barge-in), and D3 rules out standing up a package three sections early to hold ~30 lines of authorisation glue.
  - **Passes the usage string as copy** (directed check 6): states the actual purpose rather than boilerplate, and reads honestly to someone seeing the system prompt cold.
  - **Nit, not fixed:** `ContentView`'s button is disabled once status leaves `.notDetermined`, but during the in-flight `await` the status has not yet changed, so a fast double-tap could call `requestAccess` twice concurrently. Documented as safe — macOS coalesces to a single outstanding prompt — so not a defect; worth a one-line comment only to preempt the next reviewer asking.
  - **Architectural note, raised to the section supervisor:** `App/DmonHomeApp/` sits **outside** `home/Package.swift`'s target graph, so `MicrophoneAuthorizationModel.swift` has **zero** automated coverage — `dmon-home-test`'s 17/17 never touches it. Acceptable here for a thin wrapper over static platform calls, but the app target is where 5.1's log pane and 8.1's text input also land, so the untested surface grows.
- **[architect]** Ticked 3.1 and 3.2 only. **3.3 remains open** — it is the Product Owner's, and per `CLAUDE.md` a task needing human-in-the-loop verification is not tickable without their confirmation.

- **[architect]** **Task 3.3 PASSED — Product Owner confirmed 2026-08-02.** The microphone prompt appeared correctly from the built bundle. That discharges PRD §11's precondition: the TCC path is proven working *before* any audio code exists, so no later audio phase can be debugged against a silently-broken permission.
- **[architect]** New scope from the Product Owner, added as **task 3.4**: `xcodebuild` was offering an `x86_64` destination. `dmon-home` targets Apple Silicon only. Recorded as design **D15**, a new spec requirement ("The host targets Apple Silicon only", two scenarios), and an in-place amendment note on ADR-037. **The cause is worth keeping:** `home/project.yml` had *no* architecture settings, so nothing chose Intel and nothing excluded it — Xcode's default offers both. Compounding it, **ADR-034 never records the Apple Silicon constraint at all** (grepped: no arm64, no Metal, no unified memory), so the requirement that makes Intel impossible was written nowhere a reader or agent would find. The build setting is the fix; the missing record is the cause.
- **[architect]** Second Product Owner decision in the same message, and the more consequential one: **speech runs on the `dmon-home` host under any topology** — STT/TTS/VAD co-locate with the application, not the reasoner. This **resolves** the "speech sidecar location" row parked since the section-1 review, and it is right for the reason the PRD cares about: the microphone is where the person is, so raw audio never crosses the LAN on the latency-critical path. Consequence to plan for: under a split, the `dmon-home` machine must hold Parakeet plus a TTS voice, not merely drive a UI. This **refines** ADR-037 D4 rather than contradicting it — D4's rationale ("co-resident with the models they share memory pressure with") holds only in the co-located deployment.
- **[worker]** 3.4 done. **Two levers were needed, not one** — `ARCHS: arm64` in `project.yml` stops the x86_64 slice, but did **not** silence the destination warning: it shrank the list from three to two, leaving specific "My Mac" alongside generic "Any Mac". Only `-destination 'platform=macOS,arch=arm64'` on the `xcodebuild` line collapsed it. Ruled out `VALID_ARCHS` (deprecated) and `EXCLUDED_ARCHS` (indirect — subtracts from `ARCHS_STANDARD` and so depends on that macro continuing to expand to `arm64 x86_64`).
- **[reviewer]** **Approve.** Verified the two-lever necessity **empirically** rather than by reasoning: regenerated the project with `ARCHS: arm64` in place, built into an isolated derived-data path *omitting* `-destination`, and the warning returned (`grep -c` → 1) while `lipo` still reported `arm64`. So scenario 1 was satisfied by `ARCHS` alone and scenario 2 was not. Diagnosis confirmed: the ambiguity is **specific-"My Mac" vs generic-"Any Mac"**, a duality every macOS scheme carries regardless of `ARCHS`, and no `project.yml`-only lever collapses it. Ruled the `project.yml`/`Makefile` split correct rather than asymmetric — architecture is a genuine cross-surface invariant and belongs in the manifest; the destination warning is a CLI-only diagnostic and belongs on the command. Confirmed `-configuration Release` and `-derivedDataPath` byte-identical, so the path the Product Owner already verified against is intact.
  - **Two nits, both taken:** neither setting explained *why*. The reviewer's argument was that a bare `ARCHS: arm64` risks repeating the very failure that caused 3.4 — it looks like a stray override, and a future engineer could revert it to `ARCHS_STANDARD` without knowing that makes the build unusable for MLX; likewise `-destination` reads as redundant beside `ARCHS` and invites deletion. Both now carry one-line comments, squarely within `CLAUDE.md`'s "comment non-obvious constraints" rule.
- **[architect]** **Self-edit, disclosed for the supervisor to check:** the worker placed the `-destination` comment *inside* the recipe (tab-indented), where make hands it to `/bin/sh` as a no-op and echoes it into every build log — inconsistent with the unindented comment block two lines above. I moved it into that block myself rather than spend a worker round on a one-line reposition. Verified after: `make dmon-home-app` exit **0**, zero destination warnings, comment no longer echoed into the log (so the move fixed real noise, not just style), `lipo -archs` → `arm64`, `dmon-home-test` 17/17. Also removed a stray blank line my 3.4 insert left in `tasks.md`.

- **[supervisor]** Section review of `0a593d0..HEAD` (`9169062` + `95abb60`, tasks 3.1–3.4). **Request changes** — two blockers, both in ADR-037, both about the **instrument** used to record two Product Owner decisions rather than about any code. No cross-block drift, no duplicated abstraction, no dead scaffolding in the Swift or build surface.
  1. **The arm64 constraint was normative content recorded through an instrument that disclaims normative content.** Checked the ADR-028 precedent I cited: both its in-place amendments say *"framing only"* / *"terminology only"* and **relabel existing decisions**. "`dmon-home` targets Apple Silicon only" re-describes nothing — it became a `SHALL` in a binding spec and changed a build manifest in the same commit. Also **misfiled**: I attached it to D4 (the sidecar is *why* arm64 matters), but it binds **D1** (bucket/manifest/app target) and **D3** (app target/bundle id). Concrete cost — ADR-037's metadata marks the release-matrix ADRs prospectively amended "when `dmon-home` gains an artifact", and "the artifact is arm64-only" is exactly what a release-matrix reader needs; filed under STT/TTS it never reaches them.
  2. **D4's body still asserted, unqualified, a rationale this section made conditional — and my amendment qualified only half of it.** The note named D4's *second* rationale (co-residency) but not its *first*: *"two MLX runtimes competing for unified memory is worse than one socket hop"*. Under the split D15 sanctions, the reasoner is elsewhere, so there is exactly **one** MLX runtime on the `dmon-home` machine — that argument does not weaken, it **evaporates**. Design D15 even recorded the inverse consequence ("must hold Parakeet plus a TTS voice") without noticing it had knocked out D4's leading rationale. Left a binding decision with *both* stated rationales lapsed and no record of what carries it. Placement was also non-functional: the note sat ~90 lines above the text it qualified, and nobody reads an ADR top to bottom.
- **[architect]** **Product Owner approved promoting the constraint to ADR-037 Decision 5** (2026-08-02) — confirmation of wording, not substance; they had already made the call. Applied: D5 written with its own `## Consequences` bullet and picked up in the `Amends:` metadata so the release-matrix reader finds it; the amendment note retitled to speech-location only and extended to name **both** lapsed rationales plus the ground D4 actually stands on under a split (the uv-venv pattern of ADR-034, and the absence of any in-process Swift MLX path); an inline block-quote marker added at D4 itself so a reader who jumps straight to `## Decision` is not misled. `design.md` D15 tracks the restructure and now records *why the two halves took different instruments* — a refinement of rationale is an amendment; new normative content is a decision.
- **[architect]** Also taken from the supervisor's required list: the VAD clause no longer reads as though VAD lives in the sidecar (it was already host-side per D4 — the clause contradicted the decision it amended), and `specs/dmon-home/spec.md` scenario 2's `WHEN` is scoped to the app build target rather than "the command line", since the reviewer proved a bare `xcodebuild` with `ARCHS: arm64` still warns. The scenario now also asserts the guard.
- **[worker]** Remediation code fixes done. **`Makefile`: a `lipo -archs … | grep -qx arm64` guard** on the `dmon-home-app` recipe, with an explanatory comment and a readable failure message. The supervisor's point was sharper than "add a check": the causal story of 3.4 is *the constraint was written nowhere, so nothing enforced it*, and a section fixing that must not reproduce the shape one level up — the requirement's entire evidence was a human having run `lipo` once. **`ContentView.swift`: `refresh()` on `scenePhase == .active`.** Previously `refresh()` was wired to no lifecycle event, so after a denial the app told the user to enable access in System Settings and then never noticed when they did — showing launch-time status for the whole process lifetime, in a surface task 3.2 specifies as *current*. Nothing requests authorisation on any lifecycle path; only the non-prompting status read.
- **[worker]** Guard's negative case verified **honestly**: flipping `ARCHS` in the real pipeline did not produce a fat binary (xcodebuild either failed earlier or still emitted arm64-only), so rather than declare it tested, it isolated the guard's exact shell expression and ran it against synthetic `lipo -create` binaries — fat → exit 1 with message, thin x86_64 → exit 1 with message, thin arm64 → exit 0 silent. `project.yml` restored and confirmed clean before rerunning the real target.

- **[reviewer]** Request changes on the remediation. **Blocker: `design.md` D7's own Rationale paragraph still asserted the unqualified argument** — I fixed ADR-037 D4 and left design.md's own statement of the identical rationale untouched. D7 is design.md's entry point for "why a sidecar at all" and is what a future section brief would cite, so a reader hitting D7 alone met exactly the misleading text B2 existed to remove. **The fix for a defect reproduced the defect one level down.** Two nits: the `Makefile` comment *added in this very block* cited "ADR-037 **amendment**, design D15" for a constraint whose whole point is that it is **Decision 5, not an amendment** — an odd echo of the terminology bug the block was fixing (`project.yml` carried the same, predating the block); and the Relationship section still listed ADR-035 as "Builds on, unchanged" while D5's body says it makes any future artifact arm64-only for release-matrix purposes, an asymmetry with the ADR-025 D10 treatment in the metadata.
  - Verified clean: the `lipo` guard's shell semantics (the `\` continuation makes it one `/bin/sh` invocation so Make sees one exit status; `grep -qx` requires the whole line to equal `arm64` so both fat and thin-x86_64 fail; `$$(...)` correctly escaped; no `pipefail` needed since the gating signal is grep's status, not lipo's); `project.yml` genuinely restored after the worker's negative-case experiment; no lifecycle path calls `requestAccess`; the four-state switch, button gating and module-linkage proofs all intact; ADR-037 coherent to a cold reader; the spec scenario header byte-identical so `openspec archive` will not choke.
- **[architect]** All three fixed. D7 now carries a block-quote qualifying **both** lapsed reasons and naming the one that survives (the uv-venv pattern, plus the absence of an in-process Swift MLX path). Both comments now cite **Decision 5**. The Relationship entry for ADR-024/ADR-035 is re-framed as a prospective amendment of ADR-035's package→family map, matching how the metadata already treats ADR-025 D10. Swept for `ADR-037 amendment` across the Makefile, `home/`, the change dir and `docs/adrs/` — clean. Validate passes; guarded build exits 0.
- **[architect]** **The reviewer's architectural note is the durable lesson and belongs in the workflow, not just this change:** design.md "resolved" D7 via a forward pointer in the topology table (`Resolved (D15)`) while leaving D7's own prose stale. A convention that *resolving a decision always adds a backward pointer at the resolved decision's own site, not only a forward one* would have caught this automatically — and would have caught the section-1 loopback drift too, which was the same shape. Recorded in NEXT.

- **[supervisor]** Re-reviewed `0a593d0..HEAD` (`9169062` + `95abb60` + `6ddf9dd`). **Request changes — one blocker, one line.** Verified fixed: Decision 5 well-formed and reachable from three independent entry points inside the ADR; the amendment names both lapsed rationales with the surviving ground; the inline marker sits immediately after D4's rationale sentence; `design.md` D7's reproduction closed; the ADR-024/ADR-035 Relationship asymmetry removed. Ran the guard against the **real** built binary and od-verified `lipo -archs` emits exactly `arm64\n` with no trailing whitespace, so `grep -qx` is not a latent false-failure. No regressions — `-configuration Release` and `-derivedDataPath` appear only as diff context, so task 3.3's verified path is intact.
  - **Blocker: `.claude/skills/adr-index/SKILL.md` still summarised a four-decision ADR.** Not a completeness nit — **B1 half-delivered.** B1 was never "the constraint is in the wrong subsection"; it was *the constraint must be findable by the reader who needs it*. `CLAUDE.md` designates that row as the surface readers consult **instead of** opening the ADR, so the constraint sat in three places inside ADR-037 and was missing from the one artifact the workflow tells readers to trust. Exact precedent on this exact row: the section-1 reviewer blocked on it for the loopback wording with the same argument.
  - **The mechanism, which is the transferable part:** my post-remediation sweep covered *"the Makefile, `home/`, the change dir and `docs/adrs/`"* — `.claude/skills/` was not on the list. Worse, the sweep searched for the **old wording** (`ADR-037 amendment`), and this defect is a **missing addition**, which no search for stale text can ever surface. The four-round cost in this section came from **sweeps scoped by where the fix was, rather than by where the fact is duplicated.**
- **[architect]** Fixed. The ADR-037 index row now carries Decision 5 and notes that the D10/D6 artifact-source amendments are prospective and land arm64-only. Also took both `## NEXT` items while the files were open, since the ambiguity was created by this section: `ADR-037`'s Relationship bullet now reads "**ADR-028** Decisions 3–5, 7" (ADR-037 has its own Decision 5 now, so the bare reference had become ambiguous), and the guard assertion moved from the "No ambiguous destination" scenario to "The built binary is arm64 only", where the property it asserts actually lives. Scenario headers untouched, so `openspec archive`'s header matching is unaffected.
- **[architect]** Supervisor's ruling recorded so a later sweep does not "fix" it: **`home/PRD.md` Q4 must keep its unqualified rationale.** It is the open *question* that produced D4/D7 — the input, not the answer. Qualifying it would edit the question to match the answer and falsify the record of what was actually asked.
- **[supervisor]** Re-reviewed `0a593d0..HEAD` (four commits). **Approve.** Judged the index row *cold*, as the standalone artifact it is meant to be: a reader who never opens ADR-037 now gets all five decisions, with the arm64 constraint carrying its reason, its consequence, its release-matrix implication and its decision number, and the amends clause qualified where the release-matrix reader is actually looking. Both parked items taken cleanly; scenario headers verified byte-identical; guard re-verified against the real binary (`lipo -archs` emits exactly `arm64\n`). Noted approvingly that the row also picked up D4's speech-location clarification unasked — *"the same category of fact and the same reader; a row that carried D5 but not D4's location clarification would have been half-swept again."*
  - **Section 3 as a whole:** both requirements genuinely met rather than ticked — the bundle's usage string evidenced by `plutil`, sandbox absence by `codesign`, the prompt by the Product Owner; and *"targets Apple Silicon only"* is now **the stronger of the two, because scenario 1 is machine-enforced on every build** — more than most requirements in this change can say. Adding a spec requirement mid-section was handled cleanly (stable headers, satisfies-annotation, D15 recording *why* the two halves took different instruments, ADR-037 D5 making it binding). No dead scaffolding — `refresh()` now has a real lifecycle caller, closing round one's loose end rather than tolerating it. *"The section left the ADR record materially stronger than it found it, which is the opposite of the failure mode I was looking for."*
- **[supervisor]** Applied the "duplicated by design" generalisation properly — enumerated every tracked file restating four or more distinct ADRs outside `docs/adrs/` and the archive (24 files). **The index was the only authoritative one**; everything else cites ADRs as provenance for something it asserts in its own right, and citation does not go stale when a decision is added. Three findings parked in NEXT, plus `openspec/specs/package-publishing/spec.md` recorded as a **considered non-hit**: it is the natural place to expect "arm64-only", and its absence is correct because `dmon-home` has no artifact yet and D5 says the qualifier attaches *when* one lands.
- **[architect]** Acted on the graphify finding immediately rather than parking it — it was a standing `CLAUDE.md` instruction I had been skipping for three sections (*"After modifying code, run `graphify update .`"*). The graph was dated **22 July**, before this change's base commit, with **zero** `home/` nodes, while `CLAUDE.md` routes agents to it *first* for codebase questions. Every agent spawned in sections 1–3 consulted a map with the entire `home/` bucket invisible on it. Now rebuilt: 17,018 nodes / 19,230 edges, **212 `home/` nodes**. Gitignored and derived, so no commit noise. Run before section 4 rather than after, since section 4 is the largest.
- **[architect]** **Section 3 closed.** Tasks 3.1–3.4 (3.3 human-verified by the Product Owner). Blocks: 1 (3.1–3.2), 1 (3.4), plus two remediation blocks. Commits `9169062`, `95abb60`, `6ddf9dd`, `6f70e50`. Four supervisor rounds — the sweep convention written into NEXT is the durable output.

## 4. Supervision

- **[architect]** Base: `2caeee2` — a configuration-driven supervision model that adopts live children before spawning them, health-checks each on a bounded timeout, restarts crashes with backoff, starts and stops in dependency order, and kills only what it spawned — with `Dmon.Network` as the one child actually started.
- **[architect]** **Block carve — five blocks, and one of them is deliberately out of numeric order.** The section's own dependency graph, not `tasks.md`'s numbering, decides the sequence:
  - **B1 = 4.1** — the child descriptor and the full inventory. Pure model; everything else consumes it.
  - **B2 = 4.3** — bounded health checking and published health. **Pulled ahead of 4.2** because 4.2's adoption decision *is* a health check ("health-check the endpoint first, adopt when it answers"). Cutting 4.2 first would mean writing the probe twice, once throwaway.
  - **B3 = 4.2 + 4.6** — adopt-or-spawn, the process group, group-kill on exit, adoption exempt. These are one code path, not two: 4.6 is not a feature added to spawning, it is *how* spawning and killing work. Splitting them guarantees rewriting `spawn`.
  - **B4 = 4.4 + 4.5** — the policy layer over B3's primitive: crash detection with backoff, dependency-ordered startup and reverse graceful shutdown. B3 owns "terminate this child gracefully then escalate"; B4 owns *in what order* those calls are made.
  - **B5 = 4.7** — the real `Dmon.Network` child, plus verification. Expected to carry a human-in-the-loop component (relaunching the host to prove adoption); assessed when the block is carved, not assumed now.
- **[architect]** **Spawn-mechanism hazard, flagged into B3's brief rather than discovered in it.** Foundation's `Process` gives stdout/stderr pipes trivially — which **section 5.1 needs** — but offers no way to place the child in its own process group: there is no hook between fork and exec, `setpgid` from the parent races the exec, and macOS ships no `setsid(1)`. The likely answer is a small `posix_spawn` launcher (`POSIX_SPAWN_SETPGROUP` with pgroup 0, `posix_spawn_file_actions_adddup2` for the pipes, `kill(-pgid, …)` to signal the group). **This is a cross-section decision, not a B3-local one:** whatever B3 chooses must still be able to hand section 5.1 a pair of pipes, so the brief states both constraints and asks the worker to verify rather than assume.
- **[architect]** Ran `graphify update .` before carving (recorded in section 3) — the `home/` bucket is now on the map the workers consult first.
- **[architect]** **B1 (4.1) briefed.** Four calls made in the brief rather than left to the worker, because each one is the difference between a model that survives 4.2–4.7 and one that gets reworked:
  1. **"No bespoke type per child" is the requirement's teeth, and the test is mechanical:** if expressing the inventory needs a `switch` over child kind, or a subclass, or a protocol conformance per child, the model has failed. Adding a child must be adding a *value*.
  2. **Monitors must be un-spawnable by construction, not by convention.** The spec says they are "health sources that are never spawned, adopted or killed" — so the launch/adoption facet has to be *absent* from a monitor, not merely unused. A `Bool isMonitor` that every call site must remember to check is the wrong shape.
  3. **The health check is declared, not closured.** A closure would make the descriptor un-`Equatable`, un-inspectable and hostile to the tests B2 needs; a declarative case (`.http(URL)` / `.tcp(host, port)` / `.none`) keeps execution in B2 where it belongs.
  4. **Endpoints stay data.** Design D2 is explicit that the gateway endpoint is configuration and may later be a LAN hostname; `http://127.0.0.1:5500` may be a *default value* in the inventory but must not be baked into a type.
  - Also flagged the **divergence from dmonium's precedent** so the worker doesn't copy it wholesale: `ServerProcessConfig` adopts via a **PID file**; this spec adopts via an **endpoint health check**. The config-driven shape transfers; the adoption mechanism does not.

- **[worker]** B1 (4.1) done. Seven types in `Supervisor` — `ChildTransport`, `HealthCheck`, `AdoptionPolicy`, `ChildLaunch`, `ChildDescriptor`, `MonitorDescriptor`, `ChildInventory` — plus seven test files. Monitor distinction taken as a **separate struct with no launch/adoption/order facet at all**, not a flag on a shared type. `HealthCheck` a plain `Hashable` enum (`.http` / `.tcp` / `.process` / `.none`); `.process` added beyond the brief because Tailscale's real health source is a CLI invocation, not an endpoint. Endpoint defaults derived from repo evidence rather than invented. 41 tests / 11 suites green.
- **[reviewer]** **Approve with nits.** Verified at the code rather than reasoned: every endpoint default against its cited source (`DcalHealthMonitor`, `EndpointExtensions.cs`, `MlxRuntimeOptions.cs`, `DaemonController`, `TailscaleMonitor.poll()`); `.process`'s args byte-match `TailscaleMonitor.swift:93-94`; grepped `switch` across all 14 files and found no switch over child kind; grepped `unchecked` / `nonisolated(unsafe)` / `@preconcurrency` / `assumeIsolated` — zero hits; all 7 test files `import Testing`.
  - **Nit taken — `MonitorDescriptorTests.hasNoLaunchOrAdoptionFacet` is theatre.** It reflects over stored-property *name strings* to assert a fact the type checker already enforces absolutely — no code can reference `monitor.launch` whatever the test says — and it false-passes if a launch facet ever returns under a different name (`spawnConfig`). Brittle *and* redundant is the rare combination that argues for deletion rather than repair.
  - **Nit taken:** the extensibility test is named `aHypotheticalEighthChild`; there are six, so it is the seventh.
  - **Concurred with the Architect on `startsInThisChange`** independently.
- **[architect]** **Blocker the review missed, found by checking the precedent rather than the diff: `ChildLaunch` asserts knowledge the repo does not have.** Five of six descriptors carry a fabricated executable, and the sixth carries an unusable one:
  - **Gateway** — `"~/.dotnet/tools/ndmon"`. Nothing expands a leading `~`: `FileManager.isExecutableFile(atPath:)` and `Process.executableURL` both take it literally. dmonium builds the same path as `(NSHomeDirectory() as NSString).appendingPathComponent(".dotnet/tools/ndmon")` for exactly this reason. As written, task 4.7 consuming this field resolves nothing and the gateway never starts.
  - **Dcal / Dmail** — `"dcal"` / `"dmail"`. `ServiceManager.candidates` (lines 99–103) shows dmonium has **no default path for either**: only a settings override and `DMON_DCAL_SERVER_PATH` / `DMON_DMAIL_SERVER_PATH`. The honest value is not a better string, it is *"resolved from an env key"*.
  - **mlx ×2, speech sidecar** — `"mlx_lm.server"`, `"speech-sidecar"`. ADR-034 runs mlx from a uv venv, not a bare command on `PATH`; the sidecar has no implementation at all (D7).
- **[architect]** **Call: `ChildLaunch` carries an ordered candidate list, not a path** — `[ExecutableSource]` with `.environmentVariable` / `.homeRelativePath` / `.absolutePath`. This is not new design, it is **the shape dmonium already uses** (`NetworkManager.networkCandidates`: override → `DMON_NETWORK_PATH` → `~/.dotnet/tools/ndmon`) and the shape task 4.7 asks for verbatim (*"the way dmonium's `NetworkManager` does … overridable"*). It also lets a child with no decided executable say so — an **empty** candidate list — instead of naming one that does not exist. The `~` problem disappears with it: `.homeRelativePath(".dotnet/tools/ndmon")` makes expansion 4.7's mechanical job rather than string surgery over a value that looks already-resolved.
  - Weighed against the cheaper fix of just correcting the strings: there are no correct strings to write for four of them. A model that forces every descriptor to name an executable will get fiction wherever the truth is "not decided", and fiction in a static inventory is indistinguishable from fact at the call site.
- **[architect]** Carried into **B2 (4.3)**'s brief, all three from the reviewer: (1) `.process` health checks must be **run-to-completion-and-inspect-exit**, never a persistent spawn target — otherwise a monitor gains a launch path through the back door and architect call 2 has a hole in it; (2) 4.3 needs a **narrow** shared surface over both descriptor types (`id`, `healthCheck`, `timeout`) that does not smuggle `launch` back in; (3) the dmonium **PID-file vs endpoint-health** adoption divergence wants its explanatory comment where adoption is implemented — block B3 (4.2) — not here.

- **[reviewer]** **Approve** on the remediation. Verified the candidate values against the precedent line by line — gateway against `NetworkManager.swift:79-86` (correctly omitting the settings-override tier, which does not exist yet), Dcal/Dmail against `ServiceManager.swift:99-103,111-137` (`override` is always `nil` in both factories, so the env var genuinely is the only source). Grepped `home/` for `startsInThisChange`: zero hits.
  - **Reframed the test split, and the reframing is the useful part:** the *old* universal assertion — every child must declare a non-empty launch path — **was itself the root cause of the blocker.** It forced fabricating unusable paths for five children purely to stay green. So the split is a correction, not a loosening: a test demanding something untrue will be satisfied with fiction.
  - **Nit taken:** `theEnabledChildDeclaresLaunchCandidates` hardcoded `networkGateway`, so the invariant the whole round was about — *an enabled child must have real launch candidates* — was not actually guarded; flipping `dcal.isEnabled` later would not have failed anything. Now iterates `children.filter(\.isEnabled)`, with a non-vacuity guard so it cannot silently guard nothing.
- **[architect]** **Block B1 (4.1) closed.** Gates green — `make dmon-home` 0 warnings, `make dmon-home-test` 41/41, `make build` clean, `openspec validate --strict` valid. Box 4.1 ticked. Two rounds: one blocker (mine, found by checking the precedent rather than the diff), four nits.
- **[architect]** **Lesson worth carrying past this change, because it is not about this model at all:** the reviewer's first pass audited the diff thoroughly and cleanly — every endpoint default verified against its cited source — and still passed five fabricated executables, *because the diff was internally consistent and its test suite was green*. The fiction was invisible from inside: the test asserted every child declares a launch path, so five children were given one. **What caught it was reading the precedent the descriptor claimed to mirror** (`ServiceManager.candidates`) and finding it had no default path at all. A diff-local reviewer structurally cannot do that — which is the clearest illustration so far of why the reviewer and supervisor roles are not the same lens at different widths.

- **[architect]** **B2 (4.3) briefed.** Three calls, one of which is the whole point of the block:
  1. **The timeout is enforced *around* the probe seam, never inside the conformer.** dmonium bounds its probe with `request.timeoutInterval = 5` — i.e. the conformer polices itself. That cannot satisfy the spec, whose scenario is *"a check that does not complete within its timeout"* for **any** check: a conformer that hangs, or simply ignores its own timeout, defeats a self-policed bound and the scenario becomes untestable — you cannot write the failing case without a probe that misbehaves. Race the seam against the deadline in the supervisor and the guarantee holds regardless of what the conformer does.
  2. **An unimplemented check kind is `.unknown`, never `.healthy` and never `.unhealthy`.** `.tcp` and `.process` execution may be deferred (only `.http` is needed before 4.7), but a deferred kind must not report a health it did not observe. `.unhealthy` would be just as wrong as `.healthy` — it asserts an observation that was never made, and Tailscale would sit red for a reason that is not about Tailscale.
  3. **The publication seam lands in `Supervisor`, not the app target** — per the section-3 supervisor's tripwire. The spec scenario ("the host's UI reflects that child's new health state") has a package-testable half (the store publishes a change) and a shell half (SwiftUI renders it). Only the second belongs in `App/`, and it belongs in section 5.
  - Concurrency is where this block can go quietly wrong, so the brief names it: checks run **concurrently** — the scenario says a hung check must not block *other children* — with per-child deadlines, no leaked tasks on timeout, and no `DispatchSemaphore` bridging.

- **[worker]** B2 (4.3) done. `HealthCheckable` (the narrow `id`/`healthCheck`/`healthCheckTimeout` surface over both descriptor types), `TimeoutRace.withTimeout`, `HealthChecker`, `ChildHealthStore` (an `actor` publishing an `AsyncStream` of snapshots) and `HealthMonitor` (per-entity fan-out plus an injectable-interval loop). `.tcp`/`.process` deferred to `.unknown` as directed.
- **[architect]** **Blocker found by reading the seam before the audit: `HealthChecker` documented a guarantee the code cannot provide** — *"a hung or **cancellation-ignoring** probe still yields a result rather than blocking forever."* `withTaskGroup` cannot return until every child has actually finished; `cancelAll()` only trips the loser's cancellation flag. A probe that never checks it blocks `withTimeout` forever — the exact failure the bound exists to prevent. Handed to the reviewer as a claim to **confirm or refute** rather than as a verdict, since the semantics are subtle enough to be worth a second read.
- **[reviewer]** **Request changes.** Confirmed the reading and committed to a position: the **code is right and only the comment overclaims.** Under the block's own "no leaked tasks" constraint the sole way to buy an unconditional bound is a detached `Task` never awaited — which is that leak — so bounded-for-cooperatively-cancellable is the correct trade-off, and `Task.sleep` and `URLSession` are both cooperative. It also sharpened *why* this is blocker-grade: `.tcp`/`.process` are deferred to a later block, and their author would read this comment and conclude the race protects a raw socket read or a `Process.waitUntilExit()` with no cancellation hook. **`TimeoutRace.swift`'s own comment already stated the true, narrower guarantee** — the two files disagreed and `HealthChecker` was the one wrong.
  - **Second blocker, which I had missed: `aHungCheckPublishesFailureWithoutBlockingOtherChildren` could not fail for the reason it claimed.** Both "healthy" probes returned *immediately*, so a serial `for` loop also finishes inside the asserted 2s bound. The fan-out is genuinely concurrent (verified at `checkOnce`), so nothing was broken — but a regression to serial would not have been caught.
- **[worker]** Both fixed. The concurrency test now gives the healthy probes **real 150ms latency** against a 300ms bound (serial floor 350ms), and the worker **falsified it rather than asserting it**: reverted `checkOnce` to a serial loop, measured **366ms > 300ms**, restored the concurrent implementation, reran green.
- **[reviewer]** Re-audit: both fixes correct, margins independently re-derived. **But a fourth instance of the same defect class, inside the fix for the third** — the new cancellation test's comment claimed its `withTimeout(2)` wrapper guarded against `run()` regressing to non-cancellable. It cannot, for *precisely* the blocker-1 reason: `Task.value` is not a cancellation checkpoint, so under that regression the test **hangs** rather than failing at 2s.
- **[architect]** Directed the wrapper **removed rather than reworded.** It bounds only a slow-to-notice cancellation — a failure mode nobody here is worried about — and against the regression it names it hangs either way, so it buys no protection while reading as though it does. **Machinery that looks like a guard is worse than no guard: the next reader trusts it.** A plain `await task.value` plus a comment stating the real dependency is shorter and true.
- **[architect]** **Block B2 (4.3) closed.** Gates green — 54 tests in 0.161s, build 0 warnings, `make build` 0/0, validate valid. Box 4.3 ticked. Three rounds; three blockers, all of one kind.
- **[architect]** **The pattern of this section, and the thing the supervisor should weigh: four defects across two blocks, every one a claim its own tests were structurally unable to falsify.** Fabricated launch paths passed a test asserting every child declares one; a cancellation guarantee passed tests that only ever inject `Task.sleep`; a concurrency claim passed a threshold serial execution also satisfies; a cancellation guard passed because the regression it names would hang rather than fail. **None was caught by reading the diff — each was caught by asking what the test would do if the claim were false.** Worth carrying into the remaining briefs as a standing question rather than a lesson already learned: it recurred *inside the remediation for itself*, which suggests it is a property of the domain (structured concurrency, static inventories) rather than of any one worker.

- **[architect]** **B3 (4.2 + 4.6-mechanism) briefed. Four calls, and one scope split I want on the record.**
  1. **`posix_spawn`, not Foundation `Process`** — and the reason is that the two requirements collide. Section 5.1 needs **stdout/stderr pipes**, which `Process` gives free; 4.6 needs **its own process group**, which `Process` cannot do at all: there is no hook between fork and exec, `setpgid` from the parent races the exec (and fails `EACCES` once it has happened), and macOS ships no `setsid(1)`. `posix_spawn` does both — `POSIX_SPAWN_SETPGROUP` with pgroup 0, plus `adddup2` file actions.
  2. **The consequence is child reaping, and it lands on 4.4.** `Process` reaps for you and hands you `terminationHandler`; `posix_spawn` does not, so B3 owns `waitpid`/`DispatchSource` — and *that* is what 4.4's crash detection will be built on. Naming it now because it is the part of this choice that bites a later block rather than this one.
  3. **An adopted child must be structurally unkillable, not merely un-killed.** Same instinct as B1's monitors: rather than a `wasAdopted` flag every kill site must remember to check, only a **spawned** handle carries the pgid — so there is nothing to signal an adopted child *through*. The spec's "Adoption is exempt" scenario then holds by construction.
  4. **Adoption is by endpoint health check, not dmonium's PID file.** B1's reviewer parked the explanatory comment for this block; it belongs where the divergence is actually implemented.
  - **Named as a genuine footgun, because the failure is severe and silent-looking:** if `posix_spawnattr_setpgroup` is not applied, the child inherits *our* process group — and `kill(-pgid, SIGKILL)` then kills the test runner, and possibly the terminal that launched it. The brief requires asserting the child's pgid differs from our own **before** any group signal, in both the code and the tests.
- **[architect]** **Scope split: B3 ticks 4.2 only; 4.6 ticks at B5.** 4.6 says *"kill that group on **host exit**"*, and at B3 nothing in the app constructs a supervisor to hang that off — 4.7 is where the app first starts a child. Constructing one now purely to tear it down is dead scaffolding (design D3's own argument). So B3 builds and package-tests the mechanism, and B5 wires it to app termination and ticks the box. **The alternative — ticking 4.6 on a mechanism nothing calls — is exactly the "green but unfalsifiable" pattern this section has produced four times already.**

- **[worker]** B3 done. `ChildSpawner` (`posix_spawn` + `POSIX_SPAWN_SETPGROUP` + `adddup2` pipes), `ChildStartCoordinator` (the adopt-or-spawn decision over B2's `HealthChecker`), `ExecutableResolver`, `SpawnedChild`/`ChildExitStatus`, `SpawnError`, `ChildStartOutcome`, `ProcessExit.awaitExit`. **Did not take my `posix_spawn` claim on trust** — spiked it standalone, confirmed `getpgid(child) == child && != getpgrp()`, and turned up a fact I did not know: `WIFEXITED`/`WEXITSTATUS` are **function-like macros Clang will not import into Swift**, so the hand-rolled bit-math is necessary rather than stylistic.
- **[worker]** **Adopted the falsification discipline unprompted, on three separate claims** — commented out `POSIX_SPAWN_SETPGROUP` (all spawner tests failed), signalled the bare pid instead of the group (the grandchild test failed with "grandchild survived"), forced the coordinator never to adopt (the adoption test failed) — reverting each. This is the response the section's four-defect pattern was asking for.
- **[architect]** **Blocker found by reading the guard, not the tests: `precondition` was the wrong instrument at both process-group guard sites.** The double-guard *instinct* is right — a signal is irreversible, so re-checking where it fires is sound — but the trap fails differently at each site, and one of them is a genuine defect rather than a style question. Put to the reviewer with my position stated, to confirm or refute.
- **[reviewer]** **Request changes**, agreeing on both and sharpening the fix.
  1. **Spawn site:** if `POSIX_SPAWN_SETPGROUP` silently failed, the child is **already alive in our group**, and a trap does not touch it — it kills the host and leaves the child orphaned under launchd, in a group that no longer means anything. *That is exactly the "orphaned mlx process holding unified memory" failure D6 exists to prevent, manufactured by the guard meant to prevent it.* Recovery: `kill(pid, …)` is unconditionally safe (one pid, never a group), so kill, **reap**, throw.
  2. **`killProcessGroups` loop:** a trap on child 1 means children 2…n are never signalled — a guard against orphans producing a pile of them, at app-exit, when nothing further runs. A crash during quit also reads to a user as a crash-on-quit bug.
  3. **The structural point, which is the durable part:** do not fix these as two ad-hoc conditionals. Express the invariant **once**, non-trapping, and let each site own its recovery — otherwise the two guards drift apart later.
  - Both also trap in **release** (`precondition` is not `assert`), which is why they were blockers rather than nits.
- **[worker]** Fixed as specified, and **re-ran the falsification against the new failure mode** rather than assuming the old experiment still applied: no crash, every spawner test failing cleanly with `childInheritedOurProcessGroup`, and the misgrouped pid verified actually dead after the throw.
- **[reviewer]** **Approve.** Verified the shared predicate is genuinely shared with no inline re-expression; that no path throws between the kill and the reap; that all four `defer`s are function-scope and so cover the new throw; that typed throws reaches `ChildStartCoordinator` with no lossy conversion.
- **[architect]** **Two closers on top of the Approve, which interact — and I declined the reviewer's suggested test.** `SpawnedChild.init` is **public**, so `@discardableResult` on the singular `killProcessGroup` was defending a hazard that still existed: anyone can construct a child with an arbitrary pgid and silently discard a refusal. Dropped the annotation (rather than restricting the initialiser, which would have forced `@testable` on the tests).
  - **The declined test is the part worth keeping.** The reviewer proposed a mixed-array test: a deliberately-colliding `SpawnedChild` alongside a real one, asserting only the collider is refused. Consider its failure mode — if the guard is ever inverted, it does not fail, it `SIGKILL`s **the test runner's own process group**, and plausibly the terminal. **That is the one test whose failure mode is worse than the bug it guards.** Replaced with the safe instrument: `wouldSignalOurOwnGroup` made `internal` and asserted directly (`true` for `getpgrp()`, `false` otherwise), sending no signal at all — so an inverted guard fails as an assertion rather than as a dead runner. It is also *better* coverage, since that predicate is the single thing both recovery paths depend on.
  - Accepted the reviewer's reasoning that `killProcessGroups`' continue-past-refusal property no longer needs a dynamic test: with no `precondition` left in the file, early exit is a five-line control-flow fact. It needed dynamic proof only while a trap could abort the loop.
- **[architect]** **Block B3 closed. 4.2 ticked; 4.6 deliberately left unticked** — its box belongs to B5, where the shutdown is wired to app termination. Gates green: 76 tests in 0.161s, 0 warnings, `make build` 0/0, validate valid. Three rounds.

- **[architect]** **B4 (4.4 + 4.5) briefed. The headline call comes from reading the precedent, which is where B1's blocker also came from.**
  - **dmonium's backoff does not actually back off, and 4.4 must not inherit it.** `ServerProcessManager.start()` line 90 sets `currentBackoff = 2` after **every** successful start, while `handleTermination` doubles it on each crash. So for the failure that matters — a child that **launches fine and then dies immediately** — the sequence is: start (reset to 2) → crash → wait 2 → start (reset to 2) → crash → wait 2 → … **forever, at 2 seconds, never escalating.** The doubling only ever escalates when `start()` itself fails to launch, which is the *less* likely crash-loop. Reset must key on the child having **stayed up**, not on a launch having succeeded.
  - **The two tasks are one block precisely because they interact, and the interaction is a classic bug:** graceful shutdown (4.5) terminates children, and crash detection (4.4) sees a terminated child. Without an explicit intentional-stop path, **quitting the app restarts everything it just shut down.** dmonium carries an `intentionalStop` flag for exactly this. A test must prove shutdown does not trigger a restart.
  - **Restart goes back through `ChildStartCoordinator`, not straight to `spawn`.** Reattach-first is not a startup-only property (D6): if something else has taken over the endpoint by the time we retry, adopting it is the correct behaviour, and calling `spawn` directly would produce the second process adoption exists to prevent.
  - **Backoff is asserted as a *sequence of requested delays*, never as elapsed time.** A test that sleeps real backoff intervals is both slow and — per this section's four-defect pattern — passable by an incorrect sequence under a loose bound. Inject the sleep; record what was asked for.
  - **`awaitExit`'s "call once per pid" contract gets enforced structurally here** — one supervision task owning each spawned pid, not free-standing calls — since 4.4 is what makes double-reap a live failure mode rather than a documented caveat.

- **[worker]** B4 done. `RestartBackoff`, `ChildSupervisionState`, `ChildSupervisionStore`, `HostSupervisor`, plus `ProcessExitTests` (carried from B3). **Confirmed the dmonium backoff bug at the code and did not inherit it** — reset is keyed on uptime past a stability threshold, and the crash-loop case escalates `[2,4,8,16,32,60,60]` in a test that asserts the exact sequence.
- **[worker]** **Found a real production defect in B3's already-committed `ChildSpawner`: spawned children inherited the parent's blocked signal mask.** `swift test`'s runner blocks `SIGTERM`, so every spawned child inherited that block — `killProcessGroup(signal: SIGTERM)` would report success while delivering nothing, **silently breaking the whole of 4.5**. Fixed with `POSIX_SPAWN_SETSIGDEF | POSIX_SPAWN_SETSIGMASK`. The reviewer confirmed it is a genuine production defect rather than a test artifact, that the fix is complete, and that it belongs here rather than being scope creep — 4.5 is unimplementable without it.
- **[reviewer]** **Request changes — and it built a repro rather than reasoning.** `shutdown()` **deadlocked**: it wrapped `await task.value` in `withTimeout` and sent `SIGKILL` only after `withTimeout` returned, but a task group cannot return until every child finishes, and neither `Task.value` nor `awaitExit`'s `DispatchSource` continuation is a cancellation checkpoint. With a child whose whole group ignores `SIGTERM`, `shutdown()` did not return in 20s and the process needed `kill -9`. Compounding: `shutdown()` iterates sequentially, so **one stuck child stops every earlier-starting child from being asked to terminate at all** — defeating D6's own rationale.
  - **Why the escalation test could not see it, which is the instructive part:** the fixture was `trap '' TERM; sleep 30 & wait`. `kill(-pgid, SIGTERM)` reaches **every** group member, including the backgrounded `sleep 30`, which has no trap and dies on the default disposition — unblocking `wait` so the script exits **on its own** in 0.001s. The test claimed the escalation half and exercised the graceful half. The `SIGKILL` branch never ran.
  - Second blocker: the ordering test used two children whose insertion order already matched ascending `startupOrder`, so deleting the `.sorted` would not have failed it.
- **[architect]** Directed the fix at the **seam, not the call site**: `awaitExit` becomes genuinely cancellation-aware, because `withTimeout` "bounding" a structurally unboundable operation is the same fiction B2's comment fix removed. Required the rewritten test to **fail rather than hang** when escalation is removed — a hang is not evidence.
- **[worker]** Fixed, and found the missing piece: **`cancelAll()` only cancels the group's own wrapper, never an externally-referenced `Task`** — so `shutdownChild` also needs `withTaskCancellationHandler(onCancel: { task.cancel() })`. `awaitExit` now arbitrates via an `OSAllocatedUnfairLock`-backed `ExitWaitBox`. **Two further real bugs found while fixing:** `.repeatedFailure` was instantly overwritten by `.normal` on the next spawn, so it could never be observed; and the `waitUntilTrue` helpers in **both** test files swallowed `Task.sleep` cancellation with no `isCancelled` check, so a false condition polls forever regardless of its stated timeout — **the section's signature defect living in the instruments used to verify everything else.**
- **[reviewer]** **Third blocker, and the purest instance of the pattern yet — found by printing the value instead of trusting the test.** `reap()` ignored `waitpid`'s return, so a second `awaitExit` on an already-reaped pid returned **`exitCode: 0`, fabricated** (on `ECHILD` the untouched `status` of `0` reads as "exited normally"). And the test written specifically to prove that scenario safe did `_ = ...`, discarding the value — proving "doesn't hang" and nothing about honesty. **A test that structurally cannot observe what is wrong with the code it guards.** Plus: the post-`SIGKILL` reap was itself now cancellable with no fallback — *a new exposure created by the previous round's own fix* — and `.repeatedFailure` still flickered to `.normal` on every restart attempt.
- **[worker]** All three fixed. Replaced `ChildExitStatus?` with **`AwaitExitOutcome`** (`.exited` / `.cancelled` / `.reapFailed(errno:)`) rather than overloading `nil` a third time; shielded the final reap in an unstructured `Task`; gated `.normal` on `consecutiveUnstableCrashes == 0`. Each falsified.
- **[reviewer]** **Approve.** Re-ran every falsification itself: the shield test passes live (confirming **both** halves — unstructured tasks do not inherit cancellation, *and* awaiting `.value` on a `Task<_, Never>` is not a cancellation checkpoint); `errno` is read in the expression immediately after the failing `waitpid` with nothing intervening; the `AwaitExitOutcome` ripple has **no inversion** at any call site. Rated the flicker test a **structural invariant rather than a race** — with an always-crashing fixture the counter is monotonically non-decreasing, so the property holds regardless of scheduler load.
- **[architect]** Two non-gating notes taken anyway, both one-liners on ground B5 builds on: `.reapFailed` at the shutdown site was **discarded while its sibling failure three lines up published** — inconsistent with the requirement set's own "surface failure" principle; and the flicker test spent its full poll window, accounting for essentially the entire suite runtime. Now breaks out after 20 consecutive confirming samples: **0.603s → 0.088s**, suite back to 0.325s. Kept the honest `Issue.record` for the window-elapsed case.
- **[architect]** **Block B4 closed.** 4.4 and 4.5 ticked; **4.6 still deliberately unticked** for B5. Gates green: 98 tests in 0.325s, 0 warnings, `make build` 0/0, validate valid. Four rounds — each found something real, which is why the loop ran; the reviewer was told explicitly to approve with notes rather than manufacture a fifth, and did.
- **[architect]** **Block B5 brief — 4.7 + ticking 4.6: "the app owns a supervisor."** Everything B5 needs already exists as parts; **nothing composes them**. `ChildInventory.networkGateway` is already the enabled child with dmonium's candidate order (`DMON_NETWORK_PATH`, then `~/.dotnet/tools/ndmon`), `HostSupervisor` already starts/restarts/shuts down, and `ChildSpawner.killProcessGroups` already exists — **with no caller anywhere in the tree.** No `HostSupervisor` is ever constructed outside tests, `start()` is never called, and `ContentView` renders a hardcoded `ChildHealth.unknown`. So B5 is composition and lifecycle, not new supervision mechanics.
  - **Three architect calls, all made to keep the app target a shell (design D4) and to respect the section-3 testability tripwire.**
    1. **The exit sweep is a `Supervisor` method, not app-target code.** Add `HostSupervisor.terminateSpawnedProcessGroups() -> [ChildID]`, which passes every child still holding a `SpawnedChild` to `killProcessGroups` and returns the refusals. This gives `killProcessGroups` its first real caller and makes the app-exit guarantee assertable by `swift test` — an app-target-only kill loop would be exactly the untestable growth D4 forbids.
    2. **Exit is graceful-then-sweep, in that order.** `shutdown()` first (reverse order, SIGTERM→SIGKILL per child, already built and reviewed), then the sweep as a **backstop** for anything still recorded as spawned. After a clean `shutdown()` the sweep finds nothing — `handleExit` clears `spawnedChild` — and that is the point: the sweep only earns its keep when shutdown was cut short. Do not reimplement graceful termination inside the sweep.
    3. **A `HostRuntime` composition root in the package.** It owns `HostSupervisor` + `HealthMonitor` + both stores over `ChildInventory`, and exposes `start()`, `shutdownForTermination()` and a merged snapshot feed. The app target gets only an `NSApplicationDelegateAdaptor` and a thin `@MainActor @Observable` mirror.
  - **The app-termination hook must be `applicationShouldTerminate`, not `applicationWillTerminate`.** The latter is synchronous and cannot await an actor; returning `.terminateLater`, awaiting `shutdownForTermination()` under an overall budget, then calling `NSApp.reply(toApplicationShouldTerminate: true)` is the only shape that lets a bounded async shutdown complete before the process goes away. A budget is mandatory — a hung child must not wedge quit forever.
  - **Refusals must be acknowledged, per B4's carry.** `killProcessGroups` returns `[ChildID]` and is deliberately not `@discardableResult`; the delegate must at minimum log a non-empty result. That `[ChildID]` remains sufficient only while `wouldSignalOurOwnGroup` is the sole refusal reason — if this block finds a second, the return type changes with it.
  - **Scope call — a minimal health surface lands here, the log pane does not.** `ContentView`'s placeholder becomes a real per-child row (id, `ChildHealth`, `ChildSupervisionState`) fed by the stores. Two reasons: the supervision spec's *"Health is visible"* scenario has no UI anywhere in the tree today, and **4.7's human verification is not performable without it** — the Product Owner has to see adopted-vs-spawned to check the thing being verified. Streaming stdout/stderr remains 5.1's.
  - **Scope boundaries:** no changes to `ChildInventory` descriptors (4.1 settled them), no new supervision policy, no gateway *client* work (sections 6–7), no log pane (5.1), no power assertion (5.2).
  - **4.7's box does not tick on green gates.** It carries the section's only human verification — a live `ndmon` coming up on `127.0.0.1:5500` and a relaunch adopting rather than double-spawning. Worker delivers the code and a copy-pasteable recipe; the Product Owner ticks it. 4.6 ticks on gates as normal.
- **[architect]** **Running count for the supervisor: seven defects across four blocks, all one shape — a claim its own tests could not falsify.** The last three are the sharpest: an escalation test that exercised the opposite branch; a "does not hang" test that discarded the fabricated value it should have caught; and cancellation-swallowing poll helpers, i.e. **the defect inside the instruments used to verify the other defects**. Two were introduced *by remediations for earlier instances of the same shape*. The response that works is not more review passes but a specific question — *what would this test do if the claim were false?* — and the B3/B4 workers now run revert-and-confirm experiments unprompted.

- **[worker]** B5 delivered as briefed, all three architect calls intact. Notable: `HostRuntime` merges the two independently-published stores into one `[ChildStatus]` feed, and `AppDelegate` races `shutdownForTermination()` against an independent 45s budget task — neither cancels the other — with a `didReplyToTerminate` guard. **Self-declared a coverage gap rather than papering over it:** `shutdownForTerminationKillsTheSpawnedChildAndReportsNoRefusals` passes even against a sweep stubbed to `return []`, because a `sleep 30` child with no `TERM` trap dies to `shutdown()`'s own graceful SIGTERM before the sweep matters. Ran three other falsification experiments (sweep → `return []`, the enabled-filter, the health/displayName merge); all three failed correctly.
- **[reviewer]** **Approve on the code — and it tested the declared gap instead of accepting the framing.** Probed the cut-short path across delay buckets: 0ns → 8/10 left `spawnedChild` populated, 1µs → 6/10, 100µs+ → 0/10, and only 4/60 on a longer 0ns run. Cancelling the outer task propagates to **both** structured children of `withTimeout`'s `TaskGroup` simultaneously, and which one `group.next()` observes first is a real scheduler race with no seam to bias. **The worker's judgement was correct**; the existing test's docstring does not overclaim, and the sweep's own mechanism is falsifiably proven at the `HostSupervisor` level. Gap accepted as correctly disclosed.
- **[reviewer]** **One blocker, and it was a delivery gap rather than a code defect:** the 4.7 recipe existed only in the worker's report, not in `tasks.md` — a recipe the Product Owner cannot reach is not a deliverable. Plus a nit: `ChildStatusObserver`'s doc comment asserted single-observer lifetime that nothing enforced — `ContentView.init` runs the initializer expression unconditionally, `@State(initialValue:)` only *uses* the first value per view identity, and with `applicationShouldTerminateAfterLastWindowClosed` defaulting to `false`, a dock reopen constructs a second observer leaking an `AsyncStream` continuation into `HostRuntime.statusSubscribers`.
- **[architect]** Rejected the worker's first recipe draft before it reached the Product Owner: **its phase 3 could not work.** It asked for quit-then-relaunch to prove adoption, but quitting terminates the spawned gateway — which the *next* phase then proves — so the same-pid check was unsatisfiable. The worker half-noticed and hedged with a parenthetical telling the reader to relaunch "WITHOUT quitting the gateway first", leaving the Product Owner to improvise the one thing the step exists to verify. Restructured into four independent phases (cold-start spawns → quit terminates → start-manually-first adoption → adopted outlives quit), each stating what **failure** looks like. Directed the observer nit be made structurally true rather than reworded — a comment asserting an unenforced invariant is this section's signature defect in miniature.
- **[architect]** **Found the sharpest instance of the section's pattern yet, and it was in the verification recipe itself.** Phase D corroborated adoption with *"reaches healthy/normal without ever passing through `starting`"*. But the only writer to `ChildHealthStore` is `HealthMonitor.checkOnce`, forwarding `HealthChecker.check` — which returns **only** `.healthy`/`.unhealthy`/`.unknown`. **Nothing ever publishes `ChildHealth.starting`**, so a *spawned* child never passes through it either: the observation was true in both branches and discriminated nothing. Worse than a useless step — its absence reads as confirmation, to a human who has no way to know the state is unreachable. Seven defects in this section were claims their own tests could not falsify; this was the eighth, aimed at the Product Owner rather than at CI.
- **[reviewer]** **Approve (second pass).** Independently re-derived the `.starting` finding rather than taking it on report. Verified the observer fix structurally — `AppDelegate.override init()` is now the only call site, `NSApplicationDelegateAdaptor` is contract-guaranteed single-instantiation, and the three plausible double-construction paths (`App.body` re-evaluation, a second `WindowGroup` window, scene restoration) all fail to produce one. **Sharpened the "no test needed" argument:** a `HostRuntime`-level "subscriber count == 1" assertion would encode the *wrong* invariant, since multiple windows sharing one observer is legitimate by design — the guarantee genuinely belongs at the construction site, not in the package. Two advisory nits on the recipe, both closed before commit.
- **[architect]** Closed both nits. The second is worth recording: the replacement `ps -o lstart=` corroboration **overstated what it tested**. Adoption-broken can only manifest here as a *second coexisting* pid (`ChildStartCoordinator` either adopts or `posix_spawn`s fresh; no path replaces the manually-started process), which phase D's primary check already catches. `ps -o lstart=` catches same-pid-different-process, needing the kernel to reissue that exact PID — unreachable from any bug in this code. **A milder instance of the very defect it was introduced to replace**, so it was removed rather than reworded.
- **[architect]** **Block B5 closed — 4.6 ticked, 4.7 deliberately left open** pending Product Owner verification (the recipe now lives in `tasks.md` 4.7). Gates green: 104/104 Swift, `make dmon-home-app` clean arm64-only, `make build` 0/0, full .NET suite green, validate valid.

- **[architect]** **4.7 verified by the Product Owner and ticked — all four phases passed.** Cold start spawned the gateway and quit terminated it; launching against an already-running `ndmon` adopted it and quit left it running. **Section 4 is now fully ticked.**
  - **The Product Owner observed the discriminator we could not find in the code.** Adopted reported `healthy`/`normal` **immediately**; spawned took **~30s**. That is structural, not incidental: `HealthMonitor.run` calls `checkOnce` *before* its first sleep, so an adopted child's endpoint already answers on that first check, while a freshly spawned one does not answer until a later cycle. This is a genuine, observable adopted-vs-spawned signal — precisely what the deleted `ChildHealth.starting` clause was reaching for and structurally could not deliver. Worth folding into the recipe if 4.7 is ever re-run, and worth knowing before section 5 renders status.
  - It also confirms the flagged risk did not bite: 30s was sufficient for a cold `ndmon` to bind, so phase B's window needs no widening.

- **[supervisor]** **Request changes — four blockers, and the first is a production defect on the app's real quit path.**
  1. **Requirement 5 is unimplemented on the path the app actually takes.** `shutdownChild` sends `SIGTERM` to the *group* but then waits on the **leader's pid only**; the group `SIGKILL` at `HostSupervisor.swift:317` lives inside `if !exitedGracefully`. So when the leader exits promptly and a group member does not, no group kill ever fires — and `handleExit` has already cleared `spawnedChild`, removing that child from `terminateSpawnedProcessGroups()`'s input set, so the backstop cannot reach it either. **Both mechanisms miss the same case, for different reasons.** Production shape: ADR-037 has `Dmon.Network` doing per-session core spawning (`CoreProcessManager.cs:67`), so `dmoncore` children inherit `ndmon`'s group; on quit `ndmon` — with less to flush — can exit first, the host declares graceful success, and any `dmoncore` still writing `messages.jsonl` is orphaned to launchd. **Verified by the architect against the source before accepting.**
  2. **The accepted coverage gap and blocker 1 are one defect seen from two ends.** `terminateSpawnedProcessGroups()` backstops a state the app *as composed cannot produce* — `AppDelegate` deliberately never cancels the shutdown task, so the sweep's triggering condition is unreachable in production. The reviewer's race analysis was correct **given the composition**; the conclusion drawn from it was wrong. The untestable thing was not a race but **a path with no caller**. Consequence: the only test of the app's exit guarantee passes against a sweep stubbed to `return []`.
  3. **Four monitors polled forever, results consumed by nothing** — including a 30-second HTTPS request to `generativelanguage.googleapis.com`. Each half was locally right (`HostRuntime` folds monitors into the health loop; it excludes them from the status feed because a monitor has no meaningful `ChildSupervisionState`); **the combination was never decided.** `calendar-sync`, `mail` and `egress` appear nowhere in section 4 or `## NEXT` — Tailscale is the only monitor named anywhere in the section, and it declares `.process`, which `HealthChecker` returns `.unknown` for without executing. Permanently inert.
  4. **`ChildHealth.starting`, `.stopped` and `isRunning` are unreachable** — no publisher, and `isRunning` has no production reader at all.
- **[supervisor]** **The distinction that settles blocker 4, and is reusable.** `ChildDescriptor.transport` and `.endpoint` are *also* unread by production code and should **stay** — requirement 6 names them as facets a child "SHALL be described by", so a future child added purely as configuration exercises them with no code change. `ChildHealth` is not model surface, it is an **observation** type. Apply requirement 6's own extensibility test: *can a new child, added as a value only, ever produce `.starting`?* No — it needs a new writer inside `HealthChecker`/`HealthMonitor`, i.e. a code change. **Model facets earn D6's shelter; observation states do not.** Checkable rather than a matter of taste.
- **[supervisor]** **Ruling on the app-target question: the tripwire is NOT crossed** — no assertion in requirements 1–6 touches `AppDelegate`, since `HostRuntimeTests` reaches `shutdownForTermination()` directly. Declined to extract a "race N operations to one at-most-once result" utility speculatively: it would buy a tested utility with one caller and leave the same AppKit wiring behind. **Set a standing line instead:** *the app target may hold AppKit-protocol wiring and at-most-once guards; it may not hold a decision a spec scenario asserts on, nor a policy value not derived from a package type.* By that line one thing is already over it — `terminationBudget = 45`, whose own comment derives it from `gracefulShutdownTimeout` × inventory size, both of which live in `Supervisor`, with nothing keeping them in sync.
- **[supervisor]** **Instance nine, found in one of the three invariants it was asked to sample.** `sweepNeverTouchesAnAdoptedChildsBackingProcess` asserts `kill(standin.pid, 0) == 0` — but `standin` is spawned by a **separate** `ChildSpawner` and its pid is never communicated to the supervisor in any form, so **no implementation of `terminateSpawnedProcessGroups()` could kill it.** The assertion passes unconditionally while its doc comment claims *"proven with a real backing process rather than by inspecting the optional."* The test is not worthless — `currentPID == nil` and `spawnedDied` do falsify — but the assertion sold as the point does not. **Architect verified this independently before accepting it.** Verdict on the suite otherwise: single-waiter `awaitExit` ownership and spawner-level group kill are genuinely falsifiable (both confirmed by revert); ~10 of the 104 tests are enum/model tautologies, so "104/104 green" is doing rhetorical work.
- **[supervisor]** **On the generalisation — the human-facing rule is *stronger* than the test rule, and the reason is asymmetry.** For a test the observer is the runner; for a recipe it is a human. A person told to "confirm it shows `starting`" who sees nothing **assumes they mistimed the check**: absence reads as user error, not as system falsity. A failing test shouts; a failing recipe step is silently absorbed by the person running it. Two-part rule: every step names the falsifying observation *and* what it looks like when the claim is false; and **before a recipe ships, trace the publisher of every state it asks a human to look for** — mechanical, and exactly what would have caught instance eight. Belongs in standing guidance beyond this change, because **the recipe is the one artifact in the loop with no automated auditor**. Also: three of nine defects were introduced by remediations for earlier instances, which argues a remediation block should be audited against **the original claim**, not only its own diff.
- **[architect]** **Verdict accepted in full; blockers 1 and the vacuous assertion verified against the source myself rather than taken on report.** Blocker 1 is downstream of **my** B5 architect call 2 — I wrote that "after a clean `shutdown()` the sweep finds nothing… and that is the point." That was right about the sweep and wrong about the requirement: it left requirement 5's "no descendant survives" clause with no implementation on the ordinary quit path, and the reviewer and I both read the sweep's emptiness as correctness rather than as the absence of a caller.
- **[architect]** **Product Owner decision on R3 (2026-08-04): stop checking the monitors until a consumer exists.** Chosen over surfacing them now — smaller and reversible, removes unrequested third-party egress, keeps requirement 6 satisfied (which asks only that monitors be *representable*), and defers the genuine model question (how a monitor renders with no supervision state) to section 5, which has to answer it regardless. Leaves moot, for now, whether `ChildInventory.tailscale` should declare `.process` while `HealthChecker` cannot execute that kind.

- **[worker]** **Remediation block delivered — R1–R6.** R1 puts the group `SIGKILL` on `shutdownChild`'s *graceful* branch, with the POSIX pgid-reuse reasoning documented at the kill site. **Judgement call, and it went further than the brief required: `terminateSpawnedProcessGroups()` and `ChildSpawner.killProcessGroups` are DELETED**, not kept — `AppDelegate` never cancels the shutdown task, so the state they backstopped was already unreachable, and R1 closes the real gap on the ordinary path. `shutdown()`/`shutdownChild` now return `[ChildID]` of refusals directly. R2 adds the host-level grandchild test; R3 stops monitor polling per the PO decision (inventory untouched); R4 deletes the unreachable health states; R5 derives `terminationBudget` from `HostSupervisor.worstCaseShutdownDuration`; R6 replaces the vacuous sweep test. **Falsification: reverted R1, R2 failed on the full 2s poll; restored, passes in ~25ms.** Also caught a flake in its own first draft — a bare `kill(pid, 0)` after shutdown races the kernel, because once the leader is reaped the grandchild reparents to launchd and this process has no `wait()` synchronisation point for it.
- **[reviewer]** **Approve — and it verified rather than reasoned at all three points that mattered.** (1) Broke R1's branch to a bare `return nil` and confirmed R2 fails on the full 2s poll with "Expectation failed: grandchildDied", then restored byte-identically and re-ran 103/103. (2) **Validated the deletion by grepping every `.cancel()` in `home/Sources` and `home/App`** — the only cancellations are the health/forwarding tasks and `shutdownChild`'s own internal timeout — and confirmed `AppDelegate` is the sole production caller of `shutdown()`/`shutdownForTermination()`. So the argument holds: no path truncates `shutdown()` mid-flight, and the deletion is correct rather than a scope cut. (3) **Confirmed the R2 fixture avoids the exact trap block 4.5 fell into:** the leader is genuinely trap-free (`POSIX_SPAWN_SETSIGDEF`/`SETSIGMASK` reset every disposition) so it dies immediately on the group `SIGTERM`, while the grandchild's `trap '' TERM` survives the shell's tail-call `exec` **because ignored dispositions, unlike caught ones, survive `exec`** — the earlier defect put the trap on the wrong process. Also verified `waitUntilTrue` checks `Task.isCancelled` rather than relying on a `try?`-swallowed throw, so the poll is a real bound.
  - Non-obvious confirmation on R3: monitors were **never** in the merged `[ChildStatus]` feed even before this change (`supervisedIDs` was already built from `enabledChildren` only), so the polling genuinely bought nothing.
- **[architect]** **Took the reviewer's architectural note and closed it in `AppDelegate`'s doc comment** (comments are the architect's lane per the section-3 boundary; disclosed here). The note is the sharpest thing in the review: **the deletion's soundness rests on a single fact that nothing enforces** — that nothing cancels the shutdown task. It is true today by the shape of `AppDelegate` alone, not by a type or a test. Wire a `SIGTERM` handler with its own cancel-and-timeout, or change the `.terminateLater` strategy, and a child's process group can survive host exit again **with no compiler or test signal** — because the mechanism that used to catch that regression class is precisely the one this precondition justified deleting. The comment now says so at the point someone would change it.

- **[supervisor]** **Approve — section 4 closed.** All four blockers verified closed at the code, not taken on report: both `shutdownChild` branches now unconditionally `kill(-pgid, SIGKILL)`; the R2 test is genuinely falsifiable (remove the graceful-branch kill and the grandchild runs its full 30s) and is **the deterministic test the old composition could not host**; monitors decoupled with the re-enable documented; `ChildHealth` down to three cases with the writer-existence rule stated as the reason. Singled out `R6` for the part that matters: the vacuous assertion's **overclaiming comment was replaced by an honest one explaining why no such assertion could ever falsify** — *"the first time in this section a misleading comment was corrected rather than merely having its code fixed."*
- **[supervisor]** **Corrected my risk framing, and this is the substantive finding of the re-review.** I documented the deletion's precondition as "cancelling the shutdown task re-opens the hole". **It does not.** Traced with the enclosing task already cancelled: `shutdown()`'s `for` loop is not a cancellation checkpoint so it completes; the synchronous `SIGTERM` runs; inside `withTimeout` the sleep throws → `nil` → escalation branch group `SIGKILL`, *or* the operation returns `true` → graceful branch group `SIGKILL`. **Both outcomes signal the group**, so cancellation degrades to *kill everything fast*, not *skip the kill* — a direct consequence of R1 putting the kill on both branches, which means the deleted sweep's job is genuinely gone rather than merely unlikely to be needed. The real residual exposure is **aborting the loop part-way** (someone adding `try Task.checkCancellation()` inside it), which the old sweep would have caught because it iterated `states.values` rather than following the loop. Ruled: **not blocking, comment sufficient to ship** — consistent with its own refusal of a speculative seam for the termination budget — but the structural fix is one line and belongs in `## NEXT`. **`AppDelegate`'s comment rewritten to the corrected scoping**; the version I wrote would have sent the next reader chasing the wrong thing.

## 5. Log pane and power assertion

- **[architect]** Base: `a12f923` — every spawned child's stdout and stderr streamed into the app, attributed by child and retained across a restart, plus a `ProcessInfo` activity assertion held while the gateway is enabled.
- **[architect]** **Block carve — two blocks, in `tasks.md` order, because they genuinely share nothing.** B1 = 5.1 (log pane), B2 = 5.2 (power assertion). No dependency runs either way: 5.1 lands in `Supervisor` + the app's view layer, 5.2 in `Power` + two lines of app-lifecycle wiring. Cut in numeric order only because 5.1 is the larger one and the one that closes a live defect (below).
- **[architect]** **5.1 is not additive — it closes a latent deadlock that exists today.** `ChildSpawner.spawn` already wires stdout and stderr to `Pipe()`s (section 4, B3) and `SpawnedChild` already carries both read ends, but **nothing reads them.** A spawned child that writes more than a pipe buffer's worth (~64 KB) blocks in `write(2)` forever, and `ndmon` is a logging .NET host. The section-4 comments call the pipes "section 5.1 needs"; they are also section 5.1's *bug*. The brief asks for a test that proves draining, not just a test that proves attribution.
- **[architect]** **Three calls made in B1's brief rather than left to the worker.**
  1. **Buffering lives in `Supervisor`, only the SwiftUI view in the app target** — the section-4 supervisor's testability ruling, verbatim: the spec asserts *"the log pane retains the output preceding the crash"*, and that assertion must be evaluable by `swift test`.
  2. **Adopted children have no pipes and never will** — the host did not launch them, so their stdout belongs to whoever did. The pane must say so once, attributed, rather than render an empty section that reads as *"this child is silent"*. This is the Product Owner's adopted-vs-spawned discriminator arriving where it is load-bearing rather than decorative.
  3. **The buffer is bounded and the bound must be visible.** A per-child ring buffer that silently drops the oldest lines is indistinguishable, at the pane, from a child that never logged them. Dropped lines get a count.
- **[architect]** **Decision owed by this section, and my call: `ChildHealth.unhealthy` stays collapsed.** `## NEXT` parked *"timed out" vs "responded but refused"* here because the log pane was expected to be the first consumer with a reason to tell them apart. It is not one: the log pane carries **the child's own output**, and a health-check result is an observation *the host* makes about a child, not something the child said. Nothing in 5.1 reads `ChildHealth` for anything but a row label. Section 4's rule applies unchanged — **observation states earn no "built for the full inventory" shelter**, because a new state needs a new writer. Revisit at 8.3, which is the first task that genuinely asks the UI to explain a failure rather than name it.
- **[architect]** **Where 5.2's policy lives, decided ahead of the block because design D3 forbids the obvious answer.** D3 keeps `Power` a separate package with the explicit rationale that *"Supervisor should not grow a power-policy responsibility"* — so `HostRuntime` holding the assertion is out, however convenient. The shape instead: the **policy** (assertion held ⇄ gateway enabled, plus the options and reason string) lives in `Power`, driven by a plain `Bool`, with no dependency on `Supervisor`; the **fact** ("is an enabled gateway being supervised") is `Supervisor`'s and already derivable from the descriptor set; the **app target holds only the join** — a forward at launch and at terminate, no policy value of its own. Both halves of the spec scenario stay package-testable, which is what the section-4 tripwire actually demands; the two-line wire in the shell is the same category as `AppDelegate`'s existing shutdown call.

- **[architect]** **B1 (5.1) briefed.** Store shape pointed at `ChildSupervisionStore` explicitly (actor, snapshot, `updates()` with `onTermination` unregistration) so a second publishing idiom does not appear in the same package; the observer pointed at `ChildStatusObserver`'s doc comment, because the window-reopen subscriber leak it records is exactly the trap a second `@Observable` mirror walks into. Hazards called out as the reviewer's targets: the pipe-drain deadlock and its falsification, **reader tasks must never be awaited by the shutdown path** (a grandchild holding the write end means EOF never arrives), fd lifetime across restart generations, no reading inside the actor, and no claim that stdout and stderr interleave in a defined order. `ChildHealth` and the whole health/supervision model declared off-limits.

- **[worker]** **B1 (5.1) done.** `ChildLogSource`/`ChildLogLine` and a `ChildLogStore` actor (1000 lines/child, `droppedCount`) in `Supervisor`, reader tasks wired into `HostSupervisor.apply`, `logStore` exposed on `HostRuntime`, and a `ChildLogObserver` + pane in the app target. 116/116 green. **Two implementations rejected on measured evidence before the shipped one:** `FileHandle.bytes.lines` stalled mid-stream on a >64 KB write, and its replacement — a *blocking* synchronous read inside `Task.detached` — starved the cooperative thread pool, visible only when the **full** suite ran (unrelated tests stalling ~30s apiece), never under `--filter`. Shipped `DispatchIO.read` bridged through `withCheckedContinuation`. Falsified the drain test by disabling reader-task creation and watching both assertions fail. Also reported a heap-corruption crash bisected to a `[Task<Void, Never>]` field in `ChildState`, diagnosed as a Swift 6.3.3 toolchain issue and worked around with two named optional fields.
- **[architect]** **Tested two hypotheses about the new read primitive myself while the review ran, and one of them is the block's blocker.**
  - **Disproved, and worth recording because it would have been invisible:** I suspected `DispatchIO.read(fromFileDescriptor:maxLength:)` might withhold data until `maxLength` accumulated, which would mean a long-lived child like `ndmon` logging a few lines shows **nothing** in the pane until it exits — a spec failure every test in the block would still pass, because they all end at EOF. Measured against a still-running child: first line delivered in 0.00 s. The primitive is fine.
  - **Confirmed — `readChunk` is not cancellable.** Against a pipe whose write end is held by a live process that never writes: `convenience DispatchIO.read + bare withCheckedContinuation — ended within 2s of cancel: false`; `DispatchIO channel + withTaskCancellationHandler + close(flags: .stop) — ended within 2s of cancel: true`. `withCheckedContinuation` is not a cancellation checkpoint, and `drainOutput`'s `while !Task.isCancelled` only runs *between* reads.
- **[reviewer]** **Request changes — one blocker, found independently with its own repro**, and it is the same one: cancellation is inert in **exactly the case `cancelLogReaders` and both new `shutdownChild` comment blocks cite as its reason to exist** (a grandchild re-parented outside the group, so no EOF ever arrives). The reader task and its fd leak with it, because `defer { handle.closeFile() }` sits in a function body that never returns. Everything else verified rather than reasoned: reproduced the drain-test falsification itself (disabled `startLogReaders`, both assertions failed, restored byte-identically, 116/116); confirmed `dispatch_read`'s once-per-call handler contract against Apple's documentation; grepped the diff clean of `@unchecked Sendable` / `nonisolated(unsafe)` / `assumeIsolated` / `@preconcurrency`; confirmed the observer cannot reintroduce `ChildStatusObserver`'s window-reopen subscriber leak; confirmed the cap is per-`ChildID` and `droppedCount` accumulates rather than being computed once; and found **no tautological tests** — the shape section 4 shipped eight of.
  - **On the toolchain claim, which the brief singled out as the least-established thing in the block:** no active overlapping-access defect found, and the `states[id]` read-await-writeback pattern **predates this block**, so the shipped two-field shape is correct on its own merits. But the comment asserts as confirmed fact that an inert, never-written field corrupted the heap. Neither of us could establish that.
  - **Architectural note, raised for the Architect rather than blocked on:** `handleExit` nils both reader fields without cancelling, so an orphaned generation becomes unreachable — a sustained crash-restart loop with a lingering grandchild accumulates a task and two fds per crash, bounded by nothing short of app shutdown.
- **[architect]** **Fix round briefed — four items, and one reviewer suggestion overruled.**
  1. **Fix the cancellation, do not just correct the comments.** Ruled **against** the reviewer's `ExitWaitBox`-style alternative (resume the continuation early from `onCancel` behind a settled flag): it settles the continuation but leaves GCD's read outstanding on the fd, so **the leak survives the fix**, and closing the handle underneath a pending read is a double-close. The channel shape is the one I measured working. Named the fd-ownership hazard explicitly with it — `Pipe`'s `FileHandle` closes on dealloc, so a channel `cleanupHandler` that also closes is a double-close onto a possibly-recycled fd number.
  2. **Folded the reviewer's architectural note into the same round rather than parking it.** The worker's reason for not cancelling at exit-detection is sound and stands — cancelling the instant an exit is *detected* really does race genuine still-buffered trailing output. But with cancellation actually working, a **bounded drain grace** then cancel keeps the trailing output and turns an unbounded leak into a windowed one. The grace is a policy value, so it lives in the package, injectable.
  3. **Soften the toolchain comment to "suspected, not root-caused".** The failure mode of an overconfident one is symmetrical and both are expensive: a future reader spends a day chasing a toolchain bug that may not exist, or dismisses a real defect because the comment sounded settled.
  4. Surface partial data delivered alongside a read error instead of discarding it.
  - Both new tests must be falsified by actually reverting the fix, and the fd test must be deterministic or reported as not-achieved — **not tolerance-tuned into meaninglessness**.

- **[worker]** **Fix round delivered, and the blocker's root cause was worse than the shape.** `DispatchIO.read(length:)` resolves only on *length bytes or true EOF* — so a live child writing one short line then going quiet (literally the `outputSurvivesARestart` fixture's second generation) never resolved the read **at all**, cancellable or not. Rebuilding on `DispatchIO` channels fixed cancellation but not that stall. Dropped `DispatchIO` entirely for a bare `DispatchSourceRead` — fires on *any* readability — with a `ReadWaitBox` mirroring `ProcessExit.swift`'s `ExitWaitBox`. Fd ownership stated and correct: a read source only *watches* an fd, so `handle.closeFile()` in the `defer` is the sole closer. Drain grace added as an injectable `logDrainGrace` (2 s). Both new tests falsified — and falsifying the cancellation test exposed that `withTaskGroup` **cannot** bound a permanently-stuck task (it will not return until every child completes), so the bound was rebuilt as an unstructured race that does not wait on the loser. The fd test fails at exactly `initial+10` (2 fds × 5 generations) against its `initial+4` bound when the grace cancellation is removed.
- **[worker]** **Stopped on a red test rather than improvising: `repeatedImmediateCrashesRequestAnEscalatingCappedDelaySequence` now fails deterministically (5/5).** Bisected to *merely creating two `Task.detached` instances* — reproduced with empty closures, so independent of any reader code — widening a timing window in `apply`'s `.spawned` case enough for a **reentrant** `handleExit` to read `states[id]` before the outer call's writeback lands. Observed `[2, 2, 2, 4, 2, 8]` instead of `[2, 4, 8, 16, 32, 60]`. Assessed as pre-existing, requiring the exact invariant the brief declared off-limits, and asked for the Architect's call.
- **[architect]** **Call: fix it, in this block — and the defect is materially worse than the symptom that surfaced it.** Traced the lost update to its end: inside `apply`'s `.spawned` case the new child is recorded on the **local copy**, then `await store.publish(.normal,…)` releases the actor. A generation-2 crash in that window re-enters `handleExit`, which reads the stale entry and runs a **whole restart cycle to completion**, spawning generation 3 and storing its state — then generation 1's `handleExit` resumes and executes `states[id] = afterDelay`, overwriting generation 3 with a copy holding generation 2's **dead** `SpawnedChild`. Generation 3 is then live with no reference anywhere in the supervisor: `shutdownChild` reads the dead pid, returns early, and **generation 3 survives host exit and re-parents to launchd**. That is requirement 5 failing silently — **the orphaned-runtime failure design D6 exists to prevent** — and the corrupted delay sequence is the same lost update seen through its cheaper symptom (the reentrant call also reads a stale `startedAt`, so its stability verdict is computed against the wrong generation). **The instant `sleep` makes it deterministic; it does not make it possible.** In production the window is "a child that dies within a few actor hops of being spawned", which is exactly what a misconfigured `ndmon` does.
  - **In scope and mine to call, not the Product Owner's** — `HostSupervisor` is this change's own code, nothing here lies outside the change, and no other block can land ahead of this one, so the fix lands in 5.1's commit whatever we call it. **Recorded loudly because it moves section-4-approved code after section 4 closed:** the section-5 supervisor should see why, and section 4's approval should not be read as having covered it.
  - **Ruled against the worker's generation-token sketch.** A token detects a stale write after the fact and must be got right at every write site — the worker's own instinct that it has to guard the *final* write rather than entry is the argument against it. The defect class is one pattern: **a whole `ChildState` captured, carried across `await`s, and written back wholesale.** Delete the pattern (mutate `states[id]` in place, every mutation after the await preceding it) and the race has nowhere to live. Sweep scoped **by where the pattern is, not where the test pointed** — the section-3 lesson, applied to code.
  - **Test must assert the consequence, not the symptom:** the tracked child after a crash-restart storm must be the *newest* generation (`currentPID(for:)` matching the last pid actually spawned) and shutdown must signal *that* pid. A test that only re-checks the delay sequence passes against a fix that still loses the `SpawnedChild`. The existing sequence test is the canary and must go green untouched.

- **[worker]** **Re-diagnosed the canary, and it was never reentrancy.** `scheduleLogDrainGraceCancellation` — this section's own fold-in fix — reused the **same injected `sleep` closure the backoff mechanism uses**, and `logDrainGrace` defaults to `2`, *identical to the backoff's own `initial`*. So every real crash's drain-grace wait also called `recorder.record(2.0)`, salting the canary's delay sequence with extra `2.0`s indistinguishable from genuine ones. Found by call-site tracing (`callID` plus full-output capture — piped/truncated output had been actively misleading). Fixed by giving the drain grace its own `Task.sleep`: two unrelated concerns must not share a test double. **The earlier bisection — "even empty `Task.detached{}` reproduces it" — was an artifact of this same call still firing, not evidence of reentrancy.**
- **[worker]** **Reported a partial gap against the Architect's explicit ask rather than declaring victory.** The consequence test was falsified by reverting to the `inout` pattern and then run **20 solo repetitions plus a 40-concurrent-children variant, 26+ independent trials, zero failures.** The theorised window — a same-process actor hop losing a race against kernel process-exit delivery — appears never to be won on this platform: exit-detection latency exceeds the actor hop by a wide margin even under heavy scheduling contention.
- **[architect]** **Correcting my own post above: the causal claim was wrong, and the record should not be read as if it stood.** I attributed the canary's `[2, 2, 2, 4, 2, 8]` to the reentrant lost update and traced it to an orphaned generation 3. The **sequence corruption had a different and far more mundane cause** — the shared `sleep` double above. My trace described a hazard that is real *in the code*; it did not describe the failure that was actually observed, and I presented the two as the same thing. **The observed-failure half is withdrawn.**
  - **What survives, and it is only this:** `apply` recorded the new child on a local copy and then suspended at `await store.publish(…)` before the writeback, which is a genuine lost-update shape by inspection. **What does not survive is any claim that it was ever observed** — 26+ trials say the window is not won in practice. So the fix stands on a *static* argument, not an empirical one, and the honest statement of its value is "removes a hazard that could not be provoked", not "fixes an orphaned process".
  - **The fix itself still stands, on its own merits and not on my wrong story.** It removes the pattern rather than detecting its symptom, changes no observable semantics, leaves the canary byte-identical, and ships 119/119 green. Reverting it would restore a hazard to buy nothing.
  - **The lesson I am taking, and it is mine rather than the worker's:** a static trace of a concurrency hazard is a hypothesis about the code, and I let it become the explanation for a specific observed failure without testing that step. The worker's `callID` tracing did the falsification I skipped. **A plausible mechanism that predicts an observed symptom is not thereby its cause** — and this one had a competing explanation sitting inside the same round's own fold-in.
  - **Test disposition:** `aCrashRestartStormEndsWithTheSupervisorTrackingTheNewestGeneration…` is **kept**, and its comment must say plainly that it is an invariant check, **not** a falsifiable regression guard for the narrow race — it would catch a coarse break in generation tracking and nothing subtler. Recording that honestly is the whole point; a comment claiming a falsifiability it demonstrably lacks would be this change's signature defect committed in the artifact meant to close it.

- **[reviewer]** **Request changes — one blocker, and it is a comment.** Everything substantive verified, most of it by reproduction rather than reading: stripped `withTaskCancellationHandler` back to a bare continuation and watched the cancellation test fail exactly as documented; disabled the drain-grace cancellation and watched the fd test fail at `initial=4, final=14` — Δ10, exactly 2 fds × 5 generations; restored both byte-identically. Walked the in-place refactor field by field and confirmed every mutation that must land atomically happens as a synchronous burst with no `await` inside it, and that no whole-struct writeback remains anywhere in the file.
  - **Answered the independence question I asked for, and answered it before reading my correction:** the lost-update shape **was** genuinely real statically — `state.supervisionTask = Task { … }` begins running concurrently with the rest of `apply`'s own body, so a fast exit re-entering `handleExit` during `await store.publish(…)` reads the **pre-spawn** entry, because the caller's writeback is still an uncommitted local. Its trace matches mine, arrived at independently. **The refactor stands and is not reverted.**
  - **Blocker: the storm test's comment claims a falsifiability it does not have.** "Every later child's first spawn [gets] **a real chance to race** against its own crash" asserts the 40-child shape raises the odds of catching the race — the exact claim 26+ trials do not support — and the comment quotes only the 20/20 solo result while reasoning optimistically about the concurrent variant **without stating what running that variant against the reverted code produced** (nothing, same as solo). Sharpest part of the finding: the round's *other* two new tests both state their falsification as concrete quantified fact, so this is the one test that misses the standard, and the one where I had explicitly asked for it.
- **[reviewer]** **Refuted half of the worker's flake attribution rather than confirming it.** Reproduced `ChildSpawnerTests.stdoutIsCapturedThroughThePipe` stalling **30.058 s** on one run in five, then root-caused it instead of accepting "pre-existing, unrelated": `Foundation.Pipe`'s fds carry **no `FD_CLOEXEC`** (verified directly with `fcntl`), and `ChildSpawner.spawn` sets no `POSIX_SPAWN_CLOEXEC_DEFAULT` — its `file_actions` close only its own four fds. Since `swift test` runs suites as concurrent tasks **in one process**, any pipe fd held open by any test at the instant another test calls `posix_spawn` is inherited into that unrelated child. The mechanism is pre-existing and `ChildSpawner.swift` was correctly left untouched — **but "unrelated to this block" is wrong**: this round adds three long-lived pipe holders to the same binary (the storm's surviving generations, the fd test's five backgrounded `sleep 30`s, and the cancellation test's deliberately-never-closed pipe), so it raises how often the stall fires.
- **[architect]** **Call on the CLOEXEC defect: fix it, but as its own block with its own commit after 5.1 lands — not folded in.** I have already widened this block once, on a causal story that did not survive; widening it a second time in the same round is how a block stops being reviewable. The evidence here is much stronger than that one — a reproduced 30.058 s stall plus an `fcntl`-verified mechanism, not a static trace — and it is not merely a test-suite nuisance: **a child holding another child's inherited pipe write end means the first child's reader never sees EOF**, which is precisely the lifecycle this block just built. Production impact today is nil (one enabled child, nothing to inherit from), which is exactly why it can wait for its own block instead of growing this one.
- **[architect]** **Parked from the reviewer, pre-existing and unregressed:** a `shutdown()` arriving during `handleExit`'s `await coordinator.start(descriptor)` — after its `intentionalStop` guard has passed — is still not honoured until that spawn attempt completes. Identical in shape before the refactor and untouched by it. Not blocking, but it is the same *class* of gap (a spawn escaping shutdown, D6) that the refactor closed a wider instance of, so it should be on the record in miniature rather than rediscovered.

- **[worker]** Comment rewritten, nothing else touched. It now states the four things plainly, including the one that was missing: the 40-concurrent-children shape **run against the reverted, buggy code also produced zero failures**, alongside the 20/20 solo result rather than instead of it. Keeps 40 children with an honest reason — forty independent storms make a *coarse* generation-tracking regression far less likely to pass by accident — explicitly not because the count improves odds against the narrow race.
- **[architect]** **Closed the blocker myself rather than spending a third reviewer round, and disclosing that.** The fix was comment-only, its four required points were ones I dictated verbatim, and I read the result against them before accepting it. This sits inside the section-3 architect boundary (comments are my lane) with the reviewer's own acceptance criteria as the check. **Block 5.1 committed.** Gates re-run by me, not taken on report: `make dmon-home` clean, `make dmon-home-test` **119/119 in 1.5 s**, `make dmon-home-app` clean and arm64-only, `openspec validate --strict` valid, `env -u MEKO_API_KEY make test` green.

- **[architect]** **B2 briefed — the fd-inheritance fix, an unnumbered block that ticks nothing.** Ordered ahead of 5.2 deliberately: 5.2's block will run this suite, and a known 30 s flake generator should not still be in it. Brief carries the fix (`POSIX_SPAWN_CLOEXEC_DEFAULT`, verify it is exposed to Swift rather than guessing), the hazard it creates (**fd 0 would be left closed**, so the child's next `open` lands on 0 and anything writing to what it thinks is stdin corrupts that file — wire `/dev/null` explicitly), and a demand to decide out loud whether the now-redundant `addclose` calls stay.
  - **Corrected my own framing from yesterday's post before it reached the worker.** I wrote that a child holding another's inherited pipe write end breaks EOF, without checking the sequencing. `spawn` closes the parent's copy of each write end **immediately after `posix_spawn` returns**, so a *sequentially* started child cannot inherit an earlier one's write end — it no longer exists. The exposure needs **concurrent** spawn attempts (a `handleExit` restart overlapping another child's start), which is reachable but narrow, and with one child enabled today production impact is **nil**. The suite hits it because concurrent suites make the window common. The brief says so explicitly, including "do not write a comment claiming this happens on ordinary sequential startup".
  - **So the honest justification is the class, not the instance:** every descriptor this host holds is currently inherited by every child it spawns. Least privilege for spawned processes, one flag — plus a reproduced 30 s suite stall. That argument does not need the EOF story to stand up.
  - Test must be the **descriptor listing** (child reports its own `/dev/fd` while the test holds an unrelated pipe open), not the timing. Timing improvement is to be **reported if demonstrated and disclaimed if not** — an undemonstrated timing claim stated as fact is precisely this section's recurring failure.

- **[worker]** **B2 done.** `POSIX_SPAWN_CLOEXEC_DEFAULT` added — **verified genuinely exposed to Swift** via `Darwin` (`0x4000`, `sys/spawn.h:62`), so no raw-value workaround — plus an `addopen` wiring fd 0 to `/dev/null`. Kept the four now-redundant `addclose` calls on the stated grounds that they name descriptors this function itself owns by number and do not depend on an Apple-specific flag being honoured.
  - **Rejected the brief's own test suggestion, with evidence, and was right to.** `ls /dev/fd` produces **false failures**: `ls` opens the directory it is listing, landing on a low number that collides with the unrelated pipe's own low numbers — against a clean 0/1/2 table it reports `0 1 2 3 4`, two of which belong to `ls` itself. Switched to probing specific numbers with `[ -e /dev/fd/N ]`, which opens nothing. **A brief that had been followed literally would have shipped a test that fails for a reason unrelated to the defect.**
  - **Falsification with real listings, not a summary:** flag removed, child reported `write-fd-open / read-fd-open / probe-done` — *both* ends of the unrelated pipe live in the child. Restored byte-identically, passes 3× in isolation.
  - **Timing claim measured and correctly disclaimed:** 5 runs with the fix, 2.09–2.13 s, 5/5 clean; 5 without, `2.10 / 2.61 / 3.20 / 2.13 / 30.62`. The stall reproduced on `stderrIsCapturedThroughItsOwnPipeSeparateFromStdout` rather than the test the reviewer saw — read as the same *class*, not the same instance — and presented as a small-sample before/after rather than proof. This is the disclosure discipline the section has been asking for, arriving unprompted.

- **[reviewer]** **Request changes — the blocker is in the exact spot the brief sent it, and it was confirmed by repro rather than reasoning.** `posix_spawn` runs file actions strictly in order; the shipped order is `addopen(0, /dev/null)` → two `adddup2`s → four `addclose`s. Whenever the host's **own fd 0 is free** at `Pipe()` time the *read* end lands on 0, so the trailing `addclose(readEnd)` is an `addclose(0)` and **closes the `/dev/null` the earlier `addopen` just installed** — the child gets no fd 0 at all, which is precisely the empty-slot failure the wiring exists to prevent, reintroduced by the ordering. Verified with a standalone repro mirroring the sequence byte-for-byte, and again with `addopen` moved last (fd 0 correctly wired).
  - **Also ruled out the variant I would have guessed at, structurally:** a *write* end can never land on 0, because `pipe(2)` assigns the **read** end the lower of its two consecutively-allocated descriptors and 0 is the floor. So both `adddup2`s onto 1/2 are order-safe.
  - Everything else confirmed, including the two judgement calls the brief asked about: the kept `addclose`es are genuine defence-in-depth rather than divergent mechanisms (**file actions run in the forked child before `exec`; the flag only takes effect *at* `exec`** — for these four fds both produce an identical observable result), and `O_RDONLY` is the more precise choice than a shared read-write `/dev/null`. Endorsed the worker's own framing on both counts: "same class, not same instance" for the differing stall site, and "not proof" for a 1-in-5 timing sample.
- **[architect]** **Took the fix but generalised it, because the reviewer's minimal version closes the slot the repro happened to hit and leaves the class open.** A *read* end landing on **1 or 2** destroys the stdout/stderr wiring the `adddup2` just established — same shape, different slot, no repro yet. So: `addopen` moves last **and** no `addclose` may ever name 0, 1 or 2. For those slots the close is **simultaneously unnecessary and harmful** — unnecessary because `adddup2`/`addopen` onto a slot already replaces what was there, so nothing leaks; harmful because it can destroy what the file actions deliberately installed. Filter the list rather than special-casing the descriptor that bit us. **Scope the sweep by where the pattern is, not where the fix was.**
  - **Testability ruled with section 4's own precedent rather than a new argument:** do **not** test this by closing the runner's fd 0 — in a shared test process that endangers every other suite. Lift the *decision* into an `internal` pure function over the four numbers and assert the 0/1/2 collisions synthetically. This is `wouldSignalOurOwnGroup`'s argument exactly: section 4 made that `internal` so the guard could be asserted without the test's failure mode being that it `SIGKILL`s the runner's own process group. **A test's failure mode must be a failed assertion, not a broken test process.**

- **[worker]** **Blocker fixed, generalised as directed, and falsified case by case rather than in summary.** `descriptorsToCloseInChild(…) -> [Int32]` filters to `$0 > 2`; `spawn` builds its close actions from it in a loop; `addopen(0, …)` moved last as an independent second guard. Reverted the function to the old unconditional list and reported the **per-case** table — control `{3,4,5,6}` unchanged both ways, and all five collision cases differing exactly as predicted (`stdout read → 0`: `{0,3,5,6}` → `{3,5,6}`; all three slots colliding at once: `{0,1,2,7}` → `{7}`). **The control case is the part that matters most** — it is what proves the fix did not quietly change the ordinary path. 121/121.

- **[reviewer]** **Approve.** Traced all three standard slots against `spawn`'s actual execution order and confirmed the load-bearing claim: **each of 0, 1 and 2 has exactly one install action targeting it, unconditionally and by construction rather than data-dependently**, so there is no reachable case where a standard slot has no install action pointed at it — which is what makes skipping their closes leak-free. Specifically traced the generalised case I raised (a *read* end landing on 1): `adddup2(stdoutWriteFD, 1)` closes the stray copy before installing, so excluding 1 from the close list is correct **precisely because** the dup2 already handled it. Also confirmed the four pipe fds are pairwise distinct by construction, so no double-collision case exists. Independently falsified the new test against the old unconditional list — 5 issues, control passing, matching the worker's table exactly — restored byte-identically, 121/121, zero warnings on a forced clean rebuild.
  - **Control case verified byte-for-byte:** in the no-collision path the function returns all four descriptors unfiltered and the loop closes them in the same order as the four calls it replaced, so the ordinary path is genuinely untouched. This is the check that guards against over-correction, and it is the reason the generalisation was safe to make without a repro forcing it.
  - **Caveat put on the record rather than treated as a gap:** the 0/1/2 collision is proven by trace and by the reviewer's own real-`posix_spawn` repro, but not by an end-to-end test through `spawn` — forcing one means closing the runner's own standard descriptors. Precedented trade-off (`wouldSignalOurOwnGroup`), accepted, and now written down instead of rediscovered.
- **[architect]** **Took both nits myself — comment-only, disclosed per the section-3 boundary.** (1) The doc said `addopen` replaces its target "via `open(2)`'s target-replacing semantics"; POSIX specifies `addopen` as *open then duplicate onto the named descriptor*, so it is `dup2`'s semantics either way — the conclusion was right and the mechanism named was wrong, which is the kind of sentence that sends the next reader to the wrong man page. (2) Noted that `$0 > 2` also excludes negatives — unreachable from `spawn`'s own call site since `Foundation.Pipe` traps on a failed `pipe(2)`, but the filter *reads* as "standard descriptors only", so a future caller passing an unvalidated descriptor would get silent exclusion rather than a signal. Rebuilt and re-ran after editing: 121/121, valid.

## NEXT

- **Up next: section 5 — log pane and power assertion** (5.1, 5.2). **Sections 1–4 are closed, all supervisor-approved.** Both of section 5's mechanisms already exist and must be *wired, not regrown* — see the duplicated-abstraction hazard below.
- **From section 4's supervisor, carried into section 5 and beyond:**
  - **The one structural fix it recommended, not blocking but not speculative either.** The deleted process-group sweep's soundness now rests on `HostSupervisor.shutdown()`'s `for` loop always walking every child — true because the loop is not a cancellation checkpoint. Cancellation itself is *safe*: both `withTimeout` outcomes reach a group `SIGKILL`, so cancelling degrades to "kill everything fast". The exposure is someone adding `try Task.checkCancellation()` inside that loop, or rewriting it into a throwing form that bails — then children later in the reverse order are never signalled, **with no compiler or test signal**, because the sweep iterated `states.values` rather than following the loop. **One-line fix when it's wanted:** wrap `await supervisor.shutdown()` in `HostRuntime.shutdownForTermination()` in an unstructured `Task { }.value`, making the walk uncancellable by construction — the pattern already used for the shielded reap. Optionally with a test that cancels `shutdownForTermination()` immediately and asserts a `trap '' TERM` grandchild still dies. `AppDelegate`'s doc comment carries the corrected scoping.
  - **A crashed child's descendants are never group-killed — at any point, not just at shutdown.** `handleExit` clears `spawnedChild` on *every* exit including a crash, without killing the group; the restart then spawns into a *new* group and `shutdownChild` returns early because `spawnedChild` is `nil`, so survivors leak for the host's lifetime. **Pre-existing, not a regression** (the old sweep missed it identically) and not an unmet requirement — requirement 5 says "on exit" — but it is **D6's stated hazard arriving by the crash path**, and now cheap to close: `handleExit`'s non-intentional branch has `child` in hand before it clears state.
  - **Graceful-branch-under-cancellation skips the reap and the state clear.** `handleExit` never runs, so `spawnedChild` stays populated, `.stoppedIntentionally` is never published, and the `SIGKILL`ed leader is never reaped. Harmless at app exit; **matters if `shutdownForTermination()` ever gains a non-exit caller — know this before section 8.**
  - **Two comments still overclaim, named because it is this section's signature shape recurring inside the remediation** — neither worth a round: `worstCaseShutdownDurationIsGracefulTimeoutTimesEnabledChildCount` claims its values discriminate "summing instead of multiplying", but with a single per-supervisor timeout, summing over 3 children *is* 7×3 (its off-by-one discrimination is real, so the test is fine); and `HostSupervisor.swift`'s "the only other case is a fully empty group, where `kill(2)` returns `ESRCH`" — a pgid recycled between reap and kill would not `ESRCH`. Microseconds wide and needs pid wraparound, so not a hazard, but the sentence is stronger than the fact.
  - **The app-target line, set so it does not have to be re-argued in section 5 or 8:** *the app target may hold AppKit-protocol wiring and at-most-once guards; it may not hold a decision a spec scenario asserts on, nor a policy value not derived from a package type.* This sits alongside the section-3 tripwire below, and it is what sent `terminationBudget` back to the package.
  - **The human-facing falsification rule — route to the agent definitions and `openspec-apply`, not just this change.** For a test the observer is the runner; for a verification recipe it is a human, and **absence reads as user error, not system falsity** — a failing test shouts, a failing recipe step is silently absorbed by the person running it. So the rule is *stronger* than for tests, in two parts: every step names the falsifying observation **and** what it looks like when the claim is false; and **before a recipe ships, trace the publisher of every state it asks a human to look for.** Mechanical, and exactly what would have caught the `ChildHealth.starting` step. The recipe is **the one artifact in the loop with no automated auditor.**
  - **Remediation blocks get audited against the original claim, not only their own diff** — three of this section's nine defects were introduced by remediations for earlier instances of the same shape.
  - **The Product Owner's adopted-vs-spawned discriminator is real and worth using:** adopted reports `healthy` immediately, spawned takes ~30s, because `HealthMonitor.run` calls `checkOnce` before its first sleep. Unlike `ChildHealth.starting`, this is an observation the system genuinely makes — fold it into section 5's status rendering.
  - **Test-count note for judging future sections:** 93 of the 103 tests are in `SupervisorTests`, and roughly eight remain enum/model tautologies. Not worth touching; worth not counting as coverage.
- **Carry into section 10's CI brief:** `aHungCheckPublishesFailureWithoutBlockingOtherChildren` now depends on real timing — concurrent ≈150-170ms against a 300ms bound. The reviewer rated ~150ms of jitter headroom *adequate but not generous* for a loaded GitHub macOS runner. If it flakes there, the fix is to **widen the gap** (e.g. 100ms healthy latency against a 250ms bound), not to loosen the bound back to something serial execution also passes — that would restore exactly the defect this block spent a round removing.
- **Decision owed by section 5:** `ChildHealth.unhealthy` still collapses *"timed out"* and *"responded but refused"*. That satisfies the spec ("recorded as failed") and section 4 deliberately left it, but the log pane is the first consumer with a real reason to tell them apart. Decide it there rather than widening `ChildHealth` speculatively — and note section 4's ruling on what may widen it at all: **model facets earn the "built for the full inventory" shelter; observation states do not**, because a new child added purely as configuration can never produce a new observation state without a new writer.
- **Product Owner decision, 2026-08-02 — speech runs IN-PROCESS in Swift, not in a Python/mlx sidecar.** Adopting [`soniqo/speech-swift`](https://github.com/soniqo/speech-swift) (Apache 2.0, SPM, ~1.1k stars / 876 commits) with Silero VAD + Parakeet-EOU + Kokoro on CoreML. **Sequencing decided by the PO: land section 4 under the current ADR, then write the superseding ADR as its own change, informed by a spike.** It binds PRD Phases 2–3, which are outside this change entirely.
  - **This is a clean supersession, not a preference.** Section 3's remediation left ADR-037 D4 / design D7 standing on exactly two grounds: ADR-034's uv-venv pattern, and *"the absence of any in-process Swift MLX path"*. `speech-swift` **is** that path — so D7's last surviving rationale does not weaken, it is gone. It also **resolves the section-3 supervisor's parked question outright**: "the Python speech sidecar has no bucket" (D1 makes `home/` pure-Swift; the uv-venv pattern needs a .NET-package shape `home/` cannot host). In-process makes the question disappear rather than answering it. And it is the strongest available form of D15 — speech follows the host.
  - **Costs to carry into that change, all real, none fatal:** (1) **macOS 15+** is required (`MLState`, Apple's persistent ANE state API) — moves the already-landed macOS 14+ decision in **ADR-037 D3** and task 2.1; (2) `home/` gains its **first external dependency**, and the README's install snippet is `branch: "main"` with no tag — wants pinning to a revision plus a committed `Package.resolved`, and section 10's CI job starts fetching; (3) **ADR-037 D4 is binding**, so per `CLAUDE.md` the superseding ADR must be accepted *before* implementation — and per section 3's own ruling, a reversal is **new normative content, not an amendment note**.
  - **Unverified, and the spike's job:** the README says "powered by MLX **and** CoreML". Parakeet is explicitly CoreML/Neural Engine; **no backend statement was found for Kokoro or Silero**, so "all three on CoreML" is a hypothesis. Also unmeasured: latency on the target machine. `Examples/DictateDemo` is the closest reference — macOS menu-bar streaming dictation, live partials, VAD-driven EOU, Parakeet-EOU-120M + Silero VAD — and is very nearly dmon-home's dictation path already. Model weights download from HuggingFace on first use into `~/Library/Caches/qwen3-speech/`, with `offlineMode: true`, so **no Git LFS problem** — the weight-storage question that the `Dmail` ONNX precedent raised does not arise.
  - **Blast radius inside *this* change is two lines:** `ChildInventory.speechSidecar`, and the clause naming "the speech sidecar" in the supervision spec's "child model accommodates the full inventory" requirement. **Deliberately left in place** — the spec as written still names it, 4.1 must satisfy the spec it was written against, and removing a child is a value deletion, which is precisely what 4.1's model claims to make free. When the ADR lands, that claim gets tested for real.
  - **Deferred docs pass gains a member:** PRD §4.4 (Silero VAD host-side via ONNX Runtime + CoreML EP) and PRD Q4's recorded answer both change. `speech-swift` supplies VAD itself, so the ONNX Runtime dependency PRD §4.4 assumes is replaced by one dependency rather than two.
- **`.process` health checks are still unimplemented — `HealthChecker` returns `.unknown` for them without executing anything.** Section 4 established the rule they must follow when they land: **run-to-completion-and-inspect-exit, never a persistent spawn/adopt target**, or a monitor gains a launch path through the back door and 4.1's "illegal state unrepresentable" guarantee has a hole in it. `ChildInventory.tailscale` is the one descriptor declaring `.process`, and the Product Owner's Option-B monitor decision deliberately left both facts standing.
- **Deferred docs pass now has three members** (all out of scope for this change): the hard-coded ADR count (fix is *deletion*, not maintenance), the ADR-013 row/file status mismatch, and **`.serena/memories/adrs.md`** — a tracked six-ADR summary with **no consumer** (the agent definitions state there is no Serena MCP in this project). Same argument as the count: a derived duplicate nobody maintains and no reader benefits from. An orphaned ADR summary is the exact trap the index nearly became, and worse, because nothing will ever prompt anyone to update it.
- **A second by-design ADR duplication, named but not actioned:** `.claude/agents/{worker,reviewer,supervisor}.md` restate binding ADR constraints as review checklists (13, 13 and 10 distinct ADRs). Nothing is stale from section 3 — they are .NET-engine constraints and none touches ADR-037 — but they duplicate ADR content by design and are maintained by `dmons:update-scaffold` rather than by change work, so they cannot drift *from* a change and will drift *behind* one silently. This needs its own rule, not an extension of the index rule: the trigger is **a binding .NET-side ADR changing**, not any ADR gaining a decision.
- **Testability rule for the section-5 and section-8 briefs (supervisor ruling).** The untested app-target surface is **acceptable as it stands** — `home/App/DmonHomeApp/` is three small files and `MicrophoneAuthorizationModel` wraps static platform calls, so a seam around a system framework would be more machinery than the thing it guards, and an app-target test bundle costs a second scheme plus a CI change. The mitigation is not "add coverage later", it is **holding the shell discipline design D4 already states**. Both growth surfaces carry package-testable behaviour that need not land in the shell:
  - **5.1** — the log pane's *buffering* is what the spec asserts ("Output survives a child restart"). That belongs in `Supervisor`; only the SwiftUI view belongs in the app target.
  - **8.1** — the *attach-state gate* is what the spec asserts ("Input is refused when not attached"). That belongs in `GatewayClient`.
  - **Tripwire, checkable per block rather than deferred:** *if a spec scenario's assertion can only be evaluated by running the app target, the design has drifted* — and the response at that moment is a new package or an extension of an existing one, **not** a retrofitted app-target test bundle.
- **Convention worth adopting beyond this change — two halves, one judgement and one mechanical.**
  1. *(reviewer, section 3)* When a decision is **resolved or qualified** by a later one, add a **backward pointer at the resolved decision's own site**, not only a forward pointer from the resolver. design.md marked D7 "Resolved (D15)" in the topology table while leaving D7's own rationale prose stale; the section-1 loopback drift was the same shape.
  2. *(supervisor, section 3)* **Adding or renumbering a decision in any ADR requires touching that ADR's `adr-index` row in the same commit.** Fixed and enumerable rather than a judgement call, and it is the **only artifact in the repo that duplicates ADR decision content by design** — so the only one that can silently fall out of date this way. Half 1 does not catch it, because the index is not a decision site.
  The generalisation behind both, and the real lesson of this section: **scope a sweep by where the fact is duplicated, not by where the fix was.** Searching for stale wording cannot find a *missing addition* — that is why a clean `grep` for the old text coexisted with an index row that omitted an entire decision.
- **Architect boundary, named so it does not erode (supervisor).** My unreviewed `Makefile` comment reposition was judged proportionate, but the reasoning that justified it ("one line, a worker round is wasteful") reads identically for a change that *does* alter behaviour. The line: **comments and whitespace, disclosed in the DEVLOG — anything that changes what a recipe executes goes to a worker.** The `tasks.md` blank-line fix is the same category.
- **Now binding on two Product-Owner recipes, not one:** `tasks.md` 3.3 **and** 4.7 both hard-code paths derived from `-derivedDataPath home/.build-xcode` and `-configuration Release`. Switching to Debug for a faster UI loop points both recipes at a path that no longer exists, and it breaks **in the Product Owner's hands mid-verification**. A rule, not a risk: **change either flag and update both recipes in the same block.**
- **Ad-hoc signing, still live for any future app-target verification:** TCC keys grants on bundle id **plus cdhash**, so the grant is discarded on every rebuild. Any authorisation-status UI reverting to `.notDetermined` after a rebuild **must not be read as a bug** — and 4.7's recipe notes the related cost, that launching the raw binary inside the bundle (needed for the `DMON_NETWORK_PATH` override) forfeits the bundle's grant.
- **Section-10 brief must carry:** GitHub's macOS runners **do not ship `xcodegen`** — 10.1's job needs `brew install xcodegen` before it can call `make dmon-home-app`, which is otherwise the right target for it to call since it is the only path that compiles the app shell. Also weigh dropping `-quiet` in CI: errors still print, but failure diagnostics are thinner than you want in a log read after the fact.
- **Carry into the section-5 and section-6 briefs — duplicated-abstraction hazard, now spanning sections:** both mechanisms **already exist** and must be wired, not regrown. Task 5.2's assertion is `home/Sources/Power/ActivityAssertion.swift`; task 6.4's version check is `home/Sources/GatewayClient/WireVersion.swift`. Say so explicitly in each brief. 6.4 does still legitimately owe a *mismatch error* type — `WireVersion` carries only `ParseError`.
- **Carry into the section-6 brief:** the `dmon-home-gateway-client` transport-abstraction rationale was reworded in section 1 (testability first, "an alternative transport" rather than "a direct stdio core"). Task 6.1 edits that requirement — do not let the stdio framing return.
- **Carry into the section-10 brief:** word the `area-map.yml` comment (task 10.2) as **"non-.NET paths"** rather than "both Swift packages", so D7's Python speech sidecar does not require a third edit when it lands. Also: 10.1's CI job must build the **app**, not just the packages — that is the whole point of the `dmon-home-app` target.
- **Architectural notes parked by the supervisor:**
  - **`GatewayClient`'s placement may not deliver ADR-037 D2's own rationale.** D2 rationale 4 — "the local and remote clients become the same client, over the same protocol, tested once" — is load-bearing for the whole gateway-client stance. But D1 puts `GatewayClient` in `home/` as a *local* SPM package of the macOS host, and the personal iOS client (in scope per ADR-012) is a separate product that cannot depend on it there. ADR-028 D5 left `dmon-swift`, the prospective Swift *client* SDK, as an open question and this section walked past it. **Decide before task 6.1/6.2** whether `GatewayClient` is shaped and named as a shareable Swift client package, or whether rationale 4 should be read as "same protocol", not "same code". Cheap to carve out of `home/` later in SPM terms; not free in ADR terms.
  - ~~**The Python speech sidecar has no bucket.**~~ **Resolved by the Product Owner's 2026-08-02 in-process-Swift decision above** — the question disappears rather than being answered, since there is no sidecar to place. Kept as a pointer because the *reasoning* survives: `home/` is a pure-Swift bucket (D1), and the ADR-034 uv-venv pattern needs a .NET-package shape it cannot host, so any future Python-shaped dependency hits the same wall.
  - **The prospective artifact amendment may point at the wrong ADR.** ADR-037 marks ADR-025 D10 / ADR-028 D6 prospectively amended, but **ADR-035 D3/D7** is now the operative package→family map and `package-publishing`'s "App-artifact release family" is the standing requirement. Those are what actually move when `dmon-home` ships an artifact.
- **Needs the Product Owner:** **PRD §8 says "Do not set `AllowNonLoopbackBind`"** — flatly contradicted by the split-topology row in design.md, which notes a split *requires* opting into it (ADR-036 fail-closes an empty key store on a non-loopback bind). Unlike the other two split-topology rows (provisioning, speech location) this is a **security posture the Product Owner owns**, not a technical unknown. Does not block any section; must be settled before a split is attempted.
- **Open questions:**
  - **PRD Q1** — whether `mlx_lm` initialises Metal from a `LaunchDaemon` context. Does not bite while the app is foreground-launched, but constrains the always-on story. Needs a live test before any daemon split. Now also gates any revisit of Q5 (below).
- **Resolved:**
  - **Q5 — device-key provisioning ownership** (Product Owner, 2026-08-02): `dmon-home` **self-provisions**; the `ndmon` provisioning-verb alternative is not taken, so no prerequisite change lands ahead of section 6. Task 6.5 stands as written, and its pinned-digest test is what contains the two-language hash coupling. Recorded in design.md D13/Q5 with the corrected rationale: safety here rests on **process position, not network position** — unsandboxed, same user as the `devices.json` owner, loopback-only listener — *not* on the host being on a trusted LAN. If the host is ever split into a `LaunchDaemon` under a different principal, or the gateway takes a non-loopback bind (which ADR-036 fail-closes), this lapses and provisioning must be revisited.
- **Known future topology (Product Owner, 2026-08-02):** `dmon-home` and the back-end may later run on **separate machines on the same LAN** (host on a Mac mini in a living space; models on a larger machine elsewhere). Decision: **record, do not foreclose, do not build.** Sections 1–10 stay co-located. Captured in design.md → "Known future topology" with a per-concern table. Architect error caught here: design D2 said "loopback WebSocket" where PRD §7.4 already says *"reached by hostname, not assumed to be local"* — D2 now says **configured gateway endpoint**, and the PRD's stdio fallback is marked contingent on co-location (you cannot spawn a core over stdio on a machine you are not running on). Block 1.1–1.3 was stopped mid-flight to correct ADR-037 D2 before it landed with the narrowed wording. Two rows to reopen before any split is attempted: **device-key provisioning** (self-provisioning is impossible cross-machine; needs the `ndmon` verb) and **speech sidecar location** (audio is where the person is; memory is on the other box).
- **Outside this change — ADR-034 has no record of the Apple Silicon constraint.** Confirmed by grep: no `arm64`, `metal`, `unified memory`, `apple silicon`, `x86` or `intel` anywhere in it. This is the **root cause** of task 3.4 — the requirement that makes Intel impossible existed in reality and nowhere in writing, so nothing enforced it. ADR-037 D5 now records it for `dmon-home`, but ADR-034 governs the MLX runtime itself (`Dmon.Providers.Mlx`, the Daemon's triage/escalation pair, the future speech sidecar), so the next consumer that isn't `dmon-home` — a sidecar bucket decision, CI runner selection, the release matrix asking which artifacts are arch-restricted — hits the same silence and re-derives the same fact. **Own one-file change, instrument already chosen: an in-place amendment note on ADR-034.** That one *genuinely is* framing-only under the ADR-028 precedent — it states a physical fact that was always true and changes no decision — which is the instructive contrast with the arm64 blocker, where the same instrument was wrong. Keep it separate from the deferred docs pass: ADR-013's status label and the hard-coded ADR count are stale metadata; this is a **binding constraint that is currently unwritten**.
- **For the section-10 brief:** `ARCHS` is now a cross-surface invariant with two enforcement points (`home/project.yml` and the `Makefile` guard), and 10.1's CI job will be a third reader of it. The `lipo` guard living in the recipe means 10.1 **inherits it free** — do not add a parallel check.
- **Outside this change, needs its own investigation:** `WizardEngineTests.InvalidChooseOneAnswer_RePromptsStep` (`test/Dmon.Core.Tests/Rpc/WizardEngineTests.cs`) failed once under `make test` on 2026-08-02 and passed standalone and on rerun. Exercises `core/Dmon.Core/Rpc/ProviderSetupHandler.cs`. Unrelated to `dmon-home-foundations` — no block here touches .NET source. Worth a look on its own terms rather than being written off as flaky: an invalid answer intermittently failing to re-prompt could be an ordering or async-timing defect in the wizard rather than a test artifact.
- **Nits / deferred:**
  - `adr-index` frontmatter and `CLAUDE.md` both hard-code an ADR count, which drifts every time an ADR lands (it was already wrong before this block — "35" was the file count). Now correct at 32. Supervisor's verdict on disposition, which I accept: deferring it out of this section is right, but **the fix is deletion, not maintenance** — it is a derived value stored in two files, it has been wrong on two of its last two touches, and no reader benefits from it. The future docs pass should replace it with "Summaries of every accepted ADR", not re-derive a number.
  - **`make clean` cleans neither Swift tree** (supervisor, pre-existing): it is `rm -rf build/`, which misses `daemon/Daemon.App/.build/` as well as the new `home/.build/` and `home/.build-xcode/`. The new targets are therefore *consistent* with existing behaviour rather than introducing an inconsistency — noted only so it is a known gap rather than a discovery.
  - **ADR-013 status mismatch** (reviewer's note, pre-existing): the `adr-index` row says *"(Superseded by ADR-022.)"* but `docs/adrs/ADR-013-agent-profiles.md` reads `**Status:** Accepted`. The ADR-009 pair agrees across row and file; ADR-013 does not. Does not affect the 32-count. Fold into the same future docs pass.
  - **New precedent to watch** (supervisor, no action now): ADR-037 D1 says the bucket "materialises with its first member, which is the requirements document" — the first case in the repo of a **non-project file** counting as a bucket member under ADR-025's memberless rule. Contained to `home/` as written, and `monorepo-layout`'s rule text is unchanged, but the sentence is quotable if the rule is ever tested elsewhere (a stray `middleware/README.md`). Worth a look during the docs pass.
  - No release artifact for `dmon-home` — deferred until the app does something worth shipping (design D10).
- **Carry-forward:** dmonium (`daemon/Daemon.App`) must stay untouched and shipping for the whole change; its retirement is a separate change at parity (design D9, which carries the costed blast radius).
