# DEVLOG — services-security-lockdown

Running log of implementation decisions and deviations, per block. Newest block at the bottom. The architect is spawned fresh each block, so this is its cross-block memory.

## Pinned facts (read before planning any block)

- **Bind policy is a LOCAL copy, not a reference.** `services/Dmail/BindAddressPolicy.cs` is an `internal static` copy of the `frontends/Dmon.Network/NetworkBindPolicy.cs` rule (ADR-028 forbids a `services/ → frontends/` reference; the source is `internal` anyway). When the Dcal block (Group 4) lands it will be the **second consumer** of a near-identical rule — the reviewer flagged "promote to a shared `core/` package if a third consumer appears." For now Dcal does NOT bind wildcard (default Kestrel bind), so Group 4 needs only the auth+constant-time work, not a bind guard — do not add a bind policy to Dcal unless a task calls for it.
- **`Uri.TryCreate` cannot parse Kestrel wildcard hosts.** `Uri.TryCreate("http://+:8080", …)` returns false, so the bind policy uses a manual authority parser (`TryExtractHost`) instead of `System.Uri`. Any future host-parsing work in these services should reuse `BindAddressPolicy.TryExtractHost`, not `System.Uri`. Verified fail-safe: non-parseable/non-loopback hosts fall into the reject branch, so nothing Kestrel would bind to all interfaces is ever classified loopback.
- **`Dmail.csproj` has `InternalsVisibleTo("Dmail.Tests")`** — test `internal` helpers directly; do NOT boot `WebApplicationFactory` in unit tests (the app needs the ONNX model + SQLite at startup). For Group 3/5.2 auth tests, the reviewer/worker will need a strategy that avoids a full boot OR accepts an integration test that provides the model+db — flag this when planning Group 3.
- **`docker-compose.yml` disambiguation:** the ROOT `./docker-compose.yml` is the Aspire dashboard (unrelated). The Dmail one is `services/Dmail/docker-compose.yml`.
- **AUDIT.md** at repo root is untracked and deliberately NOT part of any block commit (it's the repo-wide audit that motivated the change). Stage only block files.
- **Gates:** `make build` (TreatWarningsAsErrors), `env -u MEKO_API_KEY make test` (the `env -u` avoids the live-Meko smoke hang), `openspec validate services-security-lockdown --strict`. NOTE: this machine had a severe CPU-contention spike (load 150+) during block 2 that stalled/killed full `make test` runs and crashed an MSBuild node (MSB4166) — that was host contention, not a regression. If a full `make test` stalls, check `uptime`; per-project `dotnet test` runs are a valid fallback but confirm the full suite once load settles.
- **`ApiKeyService` constructs standalone** with just `(IConfiguration, ILogger<ApiKeyService>)` — persistence/secret behaviour is unit-testable by pointing `DMAIL_DATA_DIR` at a temp dir via in-memory config, no host boot.

## Carry-over follow-ups for the Group 6 docs pass (task 6.2)

- **README/dashboard doc drift from block 2:** `services/Dmail/README.md:18` ("generated on first run and **logged once**") and `services/Dmail/wwwroot/js/dashboard.js:165` ("Auto-generated (**check server logs**)") now describe the OLD behaviour — after block 2 the key is NEVER logged, only its file path (`DMAIL_DATA_DIR/keys/api-key`). Reconcile both in the 6.2 docs pass so docs match the security posture.
- The full deploy-doc/README auth+key pass (loopback opt-in, default-deny auth, persisted-key location, residual OAuth-behind-auth flow, deferred Host-header hardening) is task 6.2 — don't pre-empt it in impl blocks.

## Block 2 — Dmail persist auto-generated key, path-only logging (tasks 2.1, 2.2, 5.3) — DONE, committed

- `services/Dmail/Services/ApiKeyService.cs`: unset-`DMAIL_API_KEY` branch now resolves `<DMAIL_DATA_DIR>/keys/api-key`; file-exists → `File.ReadAllText(path).Trim()` reuse (stable across restarts); file-absent → generate, ensure `keys/` dir, write. The leaking `LogWarning("Auto-generated API key: {Key}", …)` is GONE; replaced with one path-only `LogInformation("Auto-generated API key written to {Path}. Set DMAIL_API_KEY to override.", path)`. Configured-key branch untouched (no file, `"Using configured API key"`).
- **Atomic secret creation (folded in per reviewer):** the write uses `new FileStream(path, new FileStreamOptions { Mode = FileMode.CreateNew, Access = FileAccess.Write, UnixCreateMode = UserRead|UserWrite })` on non-Windows so `0600` is applied at creation (no `0644` umask window); plain `File.WriteAllText` fallback on Windows. **CA1416 hazard confirmed real:** every `File.SetUnixFileMode`/`GetUnixFileMode`/`UnixCreateMode` call (prod AND test) must be guarded `if (!OperatingSystem.IsWindows())` or `TreatWarningsAsErrors` fails the build. This will recur in the Dcal block (Group 4) if Dcal persists a key the same way.
- Tests (`test/Dmail.Tests/ApiKeyServiceTests.cs`): a ~20-line in-test `CapturingLogger : ILogger<ApiKeyService>` (records `formatter(state, exception)` so it catches the rendered message, not the template — asserts the key string is absent from logs). 4 new cases: persisted+not-logged, reuse-across-restart, exact-`0600`-perms (Windows-guarded), trim-on-read. Dmail.Tests 73→77.
- **Reviewer verdict:** approve, no blockers. Gates: build clean, Dmail.Tests 77/77, full `make test` green (all projects Passed!, Dmon.Core.Tests 606/607 w/ 1 pre-existing skip), validate --strict valid.

## Block 3 — Dmail default-deny auth + /health trim (tasks 3.1, 3.2, 3.3, 5.2) — DONE, committed

- **Structural default-deny auth:** `services/Dmail/ApiKeyAuthExtensions.cs` repurposed from a per-endpoint `RequireApiKey()` `RouteHandlerBuilder` filter into pipeline middleware — `UseApiKeyAuth()` registers `app.Use(InvokeAsync)`; `internal static InvokeAsync` gates on `context.Request.Path.StartsWithSegments("/api")`, resolves `ApiKeyService` from `RequestServices`, and on missing/invalid key sets 401 + `{error="unauthorized"}` and returns WITHOUT `next()` (401-before-handler). `Program.cs` wires `app.UseApiKeyAuth()` before the `MapXxx` calls — verified correct: in minimal hosting `app.Use(...)` sits between the auto-inserted `UseRouting` (start) and terminal `UseEndpoints` (end), so it runs before every handler. The 3 `.RequireApiKey()` call sites are removed (zero remain repo-wide). `StartsWithSegments("/api")` is segment-aware + case-insensitive → gates `/api`,`/api/status`,`/API/...` but NOT `/apiary`,`/health`,`/`,`/index.html`,`/js/...` (static files reachable by construction). OAuth login/callback now gated too (intended per D2; browser flow needs an authenticated client — a documented residual for 6.2).
- **/health trim (D5):** `EndpointExtensions.MapHealthHandlers` returns a new `HealthResponse` record with `[JsonPropertyName("status"/"model_loaded"/"database_ok")]` (attributes LOAD-BEARING — a bare record camelCases and breaks the wire contract). `idle_connections` dropped from both 200 and 503 branches; `/api/status` UNCHANGED (still carries `idle_connections`, now authenticated). Codes unchanged (200 healthy / 503 degraded).
- **NEW DEPENDENCY (test-only):** `Microsoft.AspNetCore.Mvc.Testing` 10.0.9 added to `Directory.Packages.props` + `test/Dmail.Tests/Dmail.Tests.csproj` (CPM-pinned, versionless ref). Present in NO shipping csproj. Added to enable the boot-level integration test (reviewer's recommendation on the core security boundary). **Surface this dependency addition to the user in the final summary** — it's the one new external dep the change introduces.
- **Tests:** `ApiKeyAuthMiddlewareTests.cs` — 15 boot-free unit tests driving `InvokeAsync` via `DefaultHttpContext` (401+no-next for the formerly-open endpoints, pass-with-key, wrong-key 401, static/`/health` open, `HealthResponse` JSON-shape). PLUS `ApiKeyAuthIntegrationTests.cs` — 3 tests booting the REAL pipeline via `WebApplicationFactory<Program>` (`Program` reachable via the existing `InternalsVisibleTo`): `/api/status` no-key→401, correct-key→not-401, `/health` no-key→200. This closes the reviewer's gap: the 401 assertion fails if the middleware is removed/mis-ordered (proves pipeline POSITION, not just delegate logic). Boot uses a new `ReadyStubEmbeddingGenerator` (384-dim via `EmbeddingService.EmbeddingDimensions`, `IsModelReady=true`) injected via `ConfigureTestServices` so no real ONNX loads; `DMAIL_API_KEY`/`DMAIL_DATA_DIR` set as process env vars + temp dir. Dmail.Tests 77→96.
- **KNOWN LATENT TEST FRAGILITY (documented, not fixed — for future test authors):** `ApiKeyAuthIntegrationTests` sets `DMAIL_API_KEY`/`DMAIL_DATA_DIR` as PROCESS-GLOBAL env vars in its ctor and unsets in Dispose. Safe TODAY because no other `Dmail.Tests` class boots a host or reads process env (siblings use `AddInMemoryCollection`). The env-var approach is deliberate: `Program.cs` reads `DMAIL_DATA_DIR` at top-level BEFORE `WebApplicationFactory`'s `ConfigureAppConfiguration` hooks apply, so the "cleaner" in-memory-config override does NOT reliably work for top-level-read vars. **If a SECOND host-booting test class is ever added, it will race this one** — at that point add `[Collection]`/disable-parallelization across the host-booting classes (do NOT just switch to `ConfigureAppConfiguration` — it won't cover the top-level `DMAIL_DATA_DIR` read). Reviewer flagged this as a non-blocking nit.
- **Reviewer verdict:** approve (block + delta re-review both clean). Gates: build clean, Dmail.Tests 96/96, full `make test` green, validate --strict valid.

## Block 4 — Dcal default-deny auth (tasks 4.1, 4.2, 4.3, 5.4) — DONE, committed

- **Ported Dmail's `ApiKeyService` to Dcal** as a service-LOCAL copy (`services/Dcal/ApiKeyService.cs`, `internal sealed`, namespace `Dcal`) — NOT a shared lib (same rationale as the BindAddressPolicy local copy). Keyed on `DCAL_API_KEY`/`DCAL_DATA_DIR`. Constant-time `Validate` (`FixedTimeEquals`), atomic-0600 `WriteKeyFile` (FileStreamOptions.UnixCreateMode, CA1416-guarded), persist-and-reuse, path-only logging. `GenerateApiKey`/`WriteKeyFile`/`Validate` copied verbatim from Dmail.
- **`DCAL_DATA_DIR` defaults to `"."`** (NOT Dmail's `/data`): Dcal is bare-metal-first (no Dockerfile; `calendar.db` already lives in CWD), so `/data` would make a bare `dotnet run` fail writing `/data/keys/`. Key persists at `./keys/api-key` next to the db. **`calendar.db` was NOT moved** (out-of-scope behaviour change). Rationale is in a code comment.
- **`services/Dcal/Program.cs`:** removed the `if (!string.IsNullOrEmpty(apiKey))` guard → middleware unconditionally installed (default-deny); `/health` the only exemption (Dcal has no static files, so "all except /health require a key" = the /api default-deny). Eager `ApiKeyService` construction after `Build()` so auto-generate/log happens at startup. `key != apiKey` → `apiKeyService.Validate(key)`. 401 body kept empty (minimal change; spec doesn't require a body). Dcal `/health` body (`lastSync`/`eventCount`) left unchanged per D5.
- **Dependency:** `test/Dcal.Tests/Dcal.Tests.csproj` adds the (already-CPM-pinned) `Microsoft.AspNetCore.Mvc.Testing` AND **removes** its pre-existing direct `Microsoft.Extensions.Logging.Abstractions` ref — Mvc.Testing pulls Logging.Abstractions ≥10.0.9 transitively, which NU1605-downgrade-conflicted with the CPM-pinned direct 10.0.8. Reviewer confirmed this is the correct minimal fix (transitive pinning is OFF; no shipping project's graph changes; `NullLogger<T>` still resolves transitively as Dmail.Tests does).
- **Tests (Dcal.Tests 13→27):** Tier A unit tests of `ApiKeyService` (mirror Dmail's, incl. `CapturingLogger`); Tier B `WebApplicationFactory<Program>` boot tests. Boot needs `DCAL_ICAL_URL` set (fail-fast) — pointed at `http://127.0.0.1:1/cal.ics` (SyncAsync catches HttpRequestException, host doesn't crash, `/api/events/*` → 503 once past auth). **Env-var race MATERIALIZED here** (the latent fragility flagged in block 3): the new host-booting class raced the existing `CalendarSyncServiceTests` on process-global `DCAL_ICAL_URL` — fixed with a shared `[Collection("DCAL env vars")]` on both classes (verified identical name; the only two classes touching those env vars).
- **Reviewer found 1 BLOCKER (B1), fixed:** the integration test originally set `DCAL_API_KEY` for EVERY case, so the no-key→401 test passed under BOTH the old guarded code and the new code — it didn't prove default-deny, and task 5.4's "with DCAL_API_KEY unset" wording wasn't exercised. Fixed by adding `EventsUpcoming_ApiKeyUnset_NoHeader_Returns401` which sets `DCAL_API_KEY=null` at boot (forcing the auto-generate branch) and asserts no-key→401 — this case fails if the `if` guard is ever reintroduced. Orchestrator verified the fix directly (read the test + confirmed the distinguishing logic) rather than a full re-review, since it exactly matched the reviewer's stated re-audit trigger.
- **INFRA LESSON (cost me an hour):** launching a full `make test` while a prior/worker full-suite run is still alive → multiple concurrent `dotnet test Everything.slnx` runs whose testhosts (WebApplicationFactory boots, shared temp state) collide and effectively hang (NOT a real deadlock — load was low). Fix: before a full-suite run, `pkill -f "Everything.slnx"`/vstest/testhost first; run ONE at a time; wrap long runs in a `sleep N && kill` watchdog so a stuck run can't burn an hour. Dcal.Tests alone runs in ~0.4s — prefer the targeted project run for iteration, full suite once for the gate.
- Gates: build clean, full `make test` green (all projects Passed!, Dcal.Tests 27/27, Dmail.Tests 96/96, Dmon.Core.Tests 606/607 w/ 1 skip, 0 failures), validate --strict valid.

## Block 5 — Final gates + security-doc reconciliation (tasks 6.1, 6.2) — DONE, committed. CHANGE COMPLETE.

- **Docs-only block** — no source/test/spec changes. Reconciled the shipped security posture into the docs:
  - `docs/deploying-dmail.md` — extended the block-1 bind note with Authentication (default-deny `/api`, `/health`+static open, 401 `{"error":"unauthorized"}`), API key persistence (`<DMAIL_DATA_DIR>/keys/api-key` 0600, path-only logged, reused; default `/data`), and OAuth (login/callback behind auth → needs authenticated admin client; Host-header hardening deferred).
  - `services/Dmail/README.md` — **fixed a security-MISLEADING** "HTTP surface" section that labelled `/api/status`, `/api/accounts` GET/DELETE, `/api/accounts/{email}/sync`, and the OAuth endpoints as "unauthenticated"/"admin (unauthenticated)" — all now correctly shown as requiring `X-Api-Key` (only `/health` + `/` + `/js/...` static open). Also fixed the `DMAIL_API_KEY` config row ("logged once" → persisted-file reality).
  - `services/Dmail/wwwroot/js/dashboard.js` — string/comment-only: `'Auto-generated (check server logs)'` → `'Auto-generated — see keys/api-key on the server'` (the key is never logged). No behaviour change.
  - `docs/deploying-dcal.md` — NEW (no Dcal README existed): default-deny auth (all routes except `/health`), key persistence `<DCAL_DATA_DIR>/keys/api-key` 0600 path-only-logged, `DCAL_DATA_DIR` default `.`; honestly states Dcal has NO bind guard.
- Standing specs (`openspec/specs/`) deliberately NOT touched — they sync at ARCHIVE time via /opsx:archive.
- **Reviewer verdict:** approve/sign-off — every doc claim cross-checked against the shipped code (both ApiKeyService log lines, ApiKeyAuthExtensions 401 body, the 10-route EndpointExtensions map, both Program.cs). No false security claims, exact identifiers, residuals honest. No nits requiring change.
- Gates: build clean, full `make test` green (verified twice independently — 20 test projects Passed!, 0 failures), validate --strict valid.

## CHANGE COMPLETE — all 6 groups / 18 tasks ticked. 5 commits (proposal + 4 impl blocks). Ready for user push/PR, then propose /opsx:archive (standing-spec sync happens then: dmail-server + dcal-sync deltas → openspec/specs/).

## Post-PR CI fix (PR #75) — Dmail integration tests + Git LFS

- **Symptom:** `Dmail.Tests.ApiKeyAuthIntegrationTests` (the 3 `WebApplicationFactory<Program>` boot tests from block 3) FAILED in CI with `OnnxRuntimeException [InvalidProtobuf] Failed to load model` at `Program.cs:45` (`AddBertOnnxEmbeddingGenerator`). Passed locally.
- **Root cause:** these are the FIRST tests that boot the real Dmail `Program`, which eagerly loads the ONNX model at registration (`AddBertOnnxEmbeddingGenerator` calls `.Create(...)` → `InferenceSession` at startup, BEFORE the test's `ConfigureTestServices` stub can replace it — the stub only ever masked post-boot behaviour, never the load). `*.onnx` is Git-LFS-tracked; CI's `actions/checkout@v4` had NO `lfs: true`, so the model was a 133-byte pointer → protobuf parse fails. `Dmail.csproj` copies the model to output, so locally (real LFS file) it boots fine. This is exactly the audit's latent **L1** ("no `lfs: true` on checkout") — my change surfaced it by being the first app-booting test.
- **Fix:** added `with: lfs: true` to the ci.yml checkout step. Minimal root-cause fix; also the correct posture (the app genuinely needs its model to boot; CI should test the real artifact). Only Dmail.Tests boots `Program` (Dcal's Program has no ONNX; `FtsSnippetIntegrationTests` uses a stub and never boots Program), so this is the complete fix. Considered but rejected: making Program.cs skip/redirect ONNX for tests (production backdoor, out of scope) — the app needs the model regardless.
- Note: CI now pulls the 69MB LFS model per run. Acceptable; the alternative (lighten the integration tests so they don't boot the real app) would forfeit the pipeline-position proof they exist for.

## Block 1 — Dmail loopback-by-default bind (tasks 1.1–1.4, 5.1) — DONE, committed

- Added `services/Dmail/BindAddressPolicy.cs` (`Validate` + `Resolve`, mirrors NetworkBindPolicy; manual `TryExtractHost` — see pinned facts for why not `Uri`). `Resolve(null,"8080",false) → http://127.0.0.1:8080`; wildcard without opt-in throws `InvalidOperationException`; wildcard with `DMAIL_ALLOW_NONLOOPBACK=true` accepted.
- `Program.cs` resolves `DMAIL_BIND_ADDRESS`/`DMAIL_ALLOW_NONLOOPBACK` from config and validates BEFORE `UseUrls`/`builder.Build()` — fail-fast, no silent fallback.
- Dockerfile sets `DMAIL_ALLOW_NONLOOPBACK=true` + `DMAIL_BIND_ADDRESS=http://+:8080` (container namespace = boundary). `services/Dmail/docker-compose.yml` publishes `127.0.0.1:${DMAIL_PORT:-8080}:8080`. New `docs/deploying-dmail.md` (minimal bind note only — full deploy-doc/auth pass is deferred to task 6.2).
- Tests: `test/Dmail.Tests/BindAddressPolicyTests.cs`, 19 cases total (12 initial + 7 reviewer-requested regression cases: no-port wildcard/loopback, userinfo `user@+`, uppercase LOCALHOST, DNS hostname both ways, IPv6 wildcard with trailing path). Dmail.Tests 73/73.
- **Reviewer verdict:** approve, no blockers; parser hand-traced fail-safe on every wildcard/loopback case. Nits (regression tests + a triple-slice cleanup) were folded in before commit. Interesting: the userinfo case `http://user@+:8080` extracts host `user@+` (not bare `+`) and is rejected via the "not loopback" branch rather than the "wildcard" branch — still fail-safe.
- No parser bug found. No ADR contradiction.
