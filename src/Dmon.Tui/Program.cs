namespace Dmon.Tui;

// Entry point — wired in Group 8 (Wiring and Verification).
internal static class Program
{
    private static void Main(string[] args)
    {
        throw new NotImplementedException("TUI host not yet wired. See Group 8 tasks.");
    }
}
